# ShiftSync — Project Memory

## Completed Core Components
- [x] Workspace scaffolded (Vite + React 19 + TypeScript strict)
- [x] Dark Luxury Speakeasy design tokens defined in src/styles/global.css
- [x] Tooling ported from prior workspace: claude-flow (ruflo), agent-reach, local Ollama bridge (scripts/ollama-exec.cjs), .claude skills and commands, .ai skills library
- [x] Master PRD placed at SHIFTSYNC_PRD.md
- [x] Consolidated master PRD created at ShiftSync/SHIFTSYNC_PRD.md (merged HOSPITALITY_SCHEDULING_GAP_ANALYSIS.md, SHIFTSYNC_UAE_WORKFORCE_RESEARCH.md, SHIFTSYNC_VALIDATION_ANALYSIS.md into one document)
- [x] Git repository initialized
- [x] Core parser engine built (src/engine/): canonical data contract (types.ts), time/date helpers (time.ts), text-block parser (parser.ts) with multi-language name/role/shift-type tokenization
- [x] Parser unit tests (src/engine/parser.test.ts) via node:test + tsx — 8 tests passing
- [x] Minimal dark-mode paste→preview→share UI wired in App.tsx (live roster grid, warnings, unparsed lines)
- [x] Psychological & behavioral framework spec written at ShiftSync/PSYCH_BEHAVIORAL_FRAMEWORK.md (cognitive-load/relief architecture for Web, Hook-model habit design for Mobile, cross-platform trust mirroring, payroll/WPS trust architecture)
- [x] Framework extended with fine-dining behavioral layer (clopening/VIP/pool-equity triggers), viral habit-loop instrumentation (K-factor + north-star metric), and behavioral risk register (10 adoption failure modes)
- [x] Feature 9: Excel Template Ingestion Engine built at `server/` (Express + xlsx + dayjs + Prisma). `server/src/parsing/templates.ts` defines the 3 master templates (STANDARD separate start/end columns, COMPACT abbreviated headers, TIME_RANGE combined "9:00 AM - 5:00 PM" column) with header-alias fuzzy matching; `parseWorkbook.ts` reads .xlsx/.xls/.csv via SheetJS, detects the template, and emits clean `ParsedShiftRow[]` + row-level issues (missing fields, unparsable dates/times, overnight-shift detection); `resolveRows.ts` matches parsed employee/role names against existing DB records per location (blocking error on unmatched Role since it's a required FK, non-blocking warning on unmatched employee → imported unassigned); `persistShifts.ts` writes confirmed rows into `Shift` inside a transaction. API: `POST /api/schedules/upload` (multipart, parses + previews, no DB writes, returns `batchId`) and `POST /api/schedules/upload/:batchId/confirm` (commits the previewed batch) in `server/src/routes/schedules.ts`, backed by an in-memory 15-min TTL cache (`server/src/store/uploadCache.ts` — note: swap for Redis before running multiple server replicas). Added `express`, `multer`, `xlsx`, `dayjs`, `cors`, `dotenv` + types to package.json, `server:dev`/`server:typecheck` scripts, isolated `server/tsconfig.json`. Verified via `npm run server:typecheck` (clean), root `typecheck`/`lint` (unaffected), and live smoke tests: parsed all 3 template shapes correctly (DD/MM/YYYY + 12h/24h times, combined ranges, overnight shifts, missing-field errors, unmatched-template rejection), and booted the Express app confirming `/api/health` and `/api/schedules/upload` validation responses. NOT yet tested against a live Postgres DB (no DATABASE_URL configured in this environment) — `resolveRowsAgainstDatabase`/`persistShifts` are unverified against real data until a DB is provisioned.
- [x] Live database provisioned: Supabase Postgres connected via the session-mode pooler (`aws-0-ap-southeast-1.pooler.supabase.com:5432`, direct `db.*.supabase.co` host was IPv6-only and unreachable from this machine). `npx prisma migrate dev --name init` applied — all 10 tables confirmed live via `prisma db pull`. Added `server/scripts/seed-test-data.ts` (idempotent: 1 Organization, 1 Location `seed-location`, 5 Roles, 2 Users) and `server/scripts/make-sample-xlsx.mjs` (generates `server/test-fixtures/sample-roster.xlsx`), with usage documented in `server/test-fixtures/README.md`. Full upload→preview→confirm flow verified live end-to-end via curl against the running `server:dev` process: parsed a real .xlsx, correctly matched 2 existing employees, flagged 1 unknown employee as `new_employee` (imported unassigned) and 1 blank-name row as a parse error, then confirmed the batch and independently re-queried Postgres directly (not just the API response) to confirm all 3 rows landed correctly with the right role/assignee/notes/status. Test shifts deleted after verification to avoid leaving fake data in the DB; seed Org/Location/Roles/Users left in place (idempotent, reusable for future testing) — the local dev server (background `tsx` process) was stopped afterward and port 4000 confirmed closed.
- [x] FIXED — timezone bug in shift persistence. Root cause: `combineDateAndTime` (`server/src/parsing/normalize.ts`) called bare `dayjs(...)`, which parses/formats wall-clock strings in the host OS's local zone; `parseDateCell`/`parseTimeCell`'s `Date`-instance branches had the same defect on the input side (SheetJS `cellDates:true` returns UTC-based `Date`s, but `dayjs(date).format()` re-renders them in host-local time). Fix: added `dayjs/plugin/utc` + `dayjs/plugin/timezone`; `combineDateAndTime(isoDate, hhmm, timezone, nextDay?)` now takes an explicit IANA timezone (via `dayjs.tz(...)`, default export `DEFAULT_VENUE_TIMEZONE = 'Asia/Dubai'`) instead of relying on host-local parsing; `parseDateCell`/`parseTimeCell` now read `Date` instances with `dayjs.utc(value)` instead of `dayjs(value)`. `persistShifts.ts` now fetches the target `Location.timezone` from the DB and threads it through both `combineDateAndTime` calls (start/end), falling back to `DEFAULT_VENUE_TIMEZONE` only if a location record somehow has no timezone set. Verified: (1) isolated unit test proved the fix is not accidental — passing `"Asia/Dubai"` vs `"UTC"` for the identical wall-clock `17:00` produced two different, individually correct UTC instants (`13:00Z` vs `17:00Z`), and the no-arg default correctly matched the `Asia/Dubai` case; (2) re-ran the full live upload→preview→confirm flow against Supabase (same fixture/seed data as before) and independently re-queried Postgres — stored UTC instants for all 3 shifts matched the correct Dubai-offset conversion; (3) `server:typecheck`, root `typecheck`, `lint`, and the full `npm test` engine suite (8/8) all pass clean with no regressions. Test shifts deleted after verification; dev server stopped, port 4000 confirmed closed.
- [x] Prisma/PostgreSQL schema authored at `prisma/schema.prisma` (7shifts-benchmarked, Dubai-adapted): Organization→Location multi-tenancy, Role + User with fixed `baseMonthlySalary` and `isActive` turnover tracking, Shift model (date/start/end/break + `managerNotes` floor directives), `ShiftSwapRequest`/`TimeOffRequest` request engine (type, target user, `expiresAt` weekly window, `RequestStatus`, `managerNote` override audit), `AuditLog`, `AttendanceLog`, and `MonthlyAttendanceSummary` for WPS/accounting rollups. `@prisma/client`+`prisma` added to package.json, `prisma:generate`/`prisma:migrate`/`prisma:studio` scripts added, `.env.example` added. Validated via `npx prisma validate` and `npx prisma generate` (both passed).

- [x] ROOT-CAUSED and FIXED the "correct shift count but shifts missing from the grid" bug, live-tested against the real Gattopardo PDF via a throwaway diagnostic script (`extractPdfText` → `parseRosterText` → live `parseRosterImage`/Gemini, deleted after use). Root cause: Andrea/Roberto/Alessandro (the unlabeled management rows with no section header above them in this file) were getting `roleName: "COVERS"` — the VLM borrowed a stray label from the unrelated covers/pax-count panel elsewhere on the sheet. `"COVERS"` isn't in `ROLE_ALIASES` and no such `Role` exists in the DB, so `resolveRows.ts` flagged it a blocking error and `persistShifts.ts` silently skipped those 15 shifts at confirm time — while the initial parse still reported a near-correct row count. Fix: `server/src/parsing/vlmPrompt.ts` role-extraction instruction now explicitly forbids borrowing a role/section label from a different part of the sheet (covers panel, legend, notes column, totals row) — a header only counts if it sits directly above a contiguous staff block in the same column; unlabeled top rows now correctly emit `role: null`. Also fixed a related bug this uncovered: `parseVision.ts` previously **dropped** any row with a null/missing role entirely (silent data loss, worse than a wrong guess); it now flows through to DB resolution and lands in the existing "Unmatched role" manual-review queue instead. `resolveRows.ts` gives a clearer message for a genuinely blank role vs. a real spelling mismatch. Live re-verified against the same PDF: Andrea/Roberto/Alessandro now correctly resolve to `"MANAGEMENT"`. Noted but not yet addressed: the same file produced 94 rows on one Gemini call and 164 on another — real run-to-run variance worth cross-validating (see Next Sprint Goals).
- [x] FIXED a second, independently-discovered hardcoded-week bug in `src/App.tsx`: the day-grid column filter used a literal `daySuffix()` map (`Mon → '08-17'`, ..., `Sun → '08-23'`) — one specific calendar week only. Any other week's roster would render **zero shifts in every day column** despite the data being present in `mergedRoster`; this was invisible only because "today" happened to fall inside that exact week. Replaced with `weekdayOf(isoDate)`, which computes the weekday label directly from each shift's own date instead of a fixed lookup.
- [x] ADDED grid-format Excel/CSV ingestion — previously `parseWorkbookBuffer` only matched 3 fixed long-format ("one row per shift") templates and hard-rejected (422) any day-of-week-grid-shaped spreadsheet (the same layout already supported for PDFs/images), even though `deterministicParser.ts`'s `parseRotaFile` existed for exactly this shape. `server/src/parsing/parseWorkbook.ts` gained `buildMergeExpandedGrid()` (reads `!merges` and copies the top-left cell's value into every covered cell, reads the range from the sheet's own `!ref` instead of a trimmed/inferred range, applied before `sheet_to_json`) and `gridToTsvText()`; `parseWorkbookBuffer` itself now uses the merge-expanded grid too (fixes vertically-merged name/role cells in long-format sheets as a side benefit). `server/src/parsing/parseVision.ts` gained `parseRosterGrid()` — mirrors `parseRosterImage`'s retry/fallback/sample structure, but sends the merge-expanded grid as a tab-separated text block to Gemini (`callGeminiWithGridText`) instead of image bytes, reusing the exact same `vlmPrompt.ts` system prompt/schema (tweaked to describe itself as accepting either an image or a text grid) — no new LLM vendor needed. On no-API-key/error it falls back to `deterministicParser.ts`'s exported `processRowsIntoRoster()` run directly on the merge-expanded grid. `server/src/routes/schedules.ts`'s Excel/CSV branch now: try `parseWorkbookBuffer` first (unchanged behavior for real long-format templates) → on `TemplateDetectionError`, build the merge-expanded grid and route through `parseRosterGrid` instead of hard-rejecting. Verified via a synthetic merged-cell `.xlsx` built with the `xlsx` library (day headers merged across AM/PM sub-columns, `SUPERVISORS`/`RUNNERS` role-section headers merged across the full staff-block width) run through the actual pipeline: `parseWorkbookBuffer` correctly rejected it (no long-format match), the merge-expansion correctly filled every merged cell, and the live Gemini call correctly extracted all 11/11 expected shifts with the right dates/times/AM-PM periods and section-header role inheritance, 0 anomalies. `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), and server tests (16/16) all clean — no regressions.

- [x] ROOT-CAUSED and FIXED the 94-vs-164 row-count variance on the Gattopardo PDF. Ran the real PDF through live `parseRosterImage` 5x (script deleted after use): mean 152.2 rows, stdev 27.8, one catastrophic outlier (97 rows, 66 anomalies) — NOT smooth temperature noise (temperature was already 0 in `callGemini`'s config). Diagnosis: the source PDF prints overnight shift end times as raw hours-past-midnight ("18 26" = 18:00 to 2am next day); in the bad run the model echoed "26:00"/"25:00"/"27:00" literally into the `endTime` field instead of converting to true wall-clock time, and the pipeline's time validator correctly rejects any hour ≥24 — which explains the near-uniform ~50% per-employee count drop (every overnight shift that run became an anomaly instead of a row) and the anomaly spike. Fix: added a regex `pattern` (`^$|^([01][0-9]|2[0-3]):[0-5][0-9]$`) to `startTime`/`endTime` in `ROSTER_VLM_GEMINI_SCHEMA` (`server/src/parsing/vlmPrompt.ts`) — Gemini's structured output enforces schema compliance during generation, so an hour ≥24 is now structurally impossible to emit, not just discouraged by prompt wording. Also added an explicit "OVERNIGHT-ROLLOVER RULE" paragraph to the SHIFT INTERPRETATION section explaining the conversion, and applied the same pattern to the dead/unused OpenAI-schema variant for consistency. Re-ran the same 5x live test after the fix: mean 167.6, stdev 2.9, **0 anomalies across all 5 runs**, 11/16 employees with byte-identical shift counts on every single run (the remaining 5 employees varied by only 1 shift, consistent with a few genuinely ambiguous cells like unmarked "Close" times, not a systemic failure). `npm run server:typecheck` clean.
- [x] BUILT and ran 3 synthetic stress-test rosters 3x each (in addition to the days-as-columns/merged-headers shape already validated with 11/11 correct) to check the grid-ingestion pipeline's format generality, each with hand-defined ground truth and an automated pass/fail diff: (B) days-as-rows/staff-as-columns with name+role combined in the header cell — **2/3 runs pass**; (C) flat one-row-per-shift list with headers deliberately not matching any of the 3 long-format templates, forcing the grid-text VLM fallback — **2/3 runs pass**; (D) no header row + irregular date formats ("17/8", "21 Aug") plus one deliberately invalid date ("31/2") to check it's flagged rather than silently hallucinated into a real shift — **3/3 runs pass**, including the invalid date correctly surfacing as an anomaly with a clear reason instead of becoming a phantom shift. The B/C failures were investigated directly (not assumed): re-running Test C's exact failing input in isolation immediately afterward succeeded fully both with and without the new `startTime`/`endTime` schema pattern constraint (`finishReason: STOP` both times, i.e. a clean/voluntary completion, not truncation) — confirming the schema fix above did NOT cause these failures. The B/C failures are themselves smaller-scale instances of the same run-to-run non-determinism investigated in the entry above (the model occasionally stops after the first employee/row-group on a fresh call, unrelated to input structure), not a structural inability to parse the days-as-rows or flat-list shapes specifically. This raises the priority of the double-pass cross-validation idea in Next Sprint Goals — it would catch exactly this failure mode on any format, not just the one PDF.

- [x] PIVOTED the roster-parsing architecture: deterministic parsing is now the PRIMARY path for Excel/CSV, LLM (Gemini) is the fallback/last-resort instead of the default — per-upload decision to stop depending on single-shot LLM reliability for the majority of uploads (Excel exports, digital files), which have real cell-grid structure a model shouldn't need to "read" at all. Scoped to stay Node/TypeScript-only (no Python/openpyxl/pdfplumber subprocess) — SheetJS already gave full cell-grid+merge access via last session's `buildMergeExpandedGrid()`; a Python bridge would have added a second runtime/deployment story for no functional gain on the Excel side. PDF gets a Node-native positional table reconstruction (pdfjs-dist) as a separate follow-up, since Node has no pdfplumber-equivalent with built-in table detection.
  - New `server/src/parsing/deterministicGridParser.ts` (`parseExcelGrid`) — reads a merge-expanded grid directly (no network call, fully reproducible) for day-grid rosters (day-of-week columns, optional AM/PM sub-columns, role-section header rows). Encodes both real bugs found in the LLM path as structural rules instead of prompt hopes: (1) a role/section header only ever applies to the staff rows that follow it in the same staff-name column — detects BOTH merge-expansion shapes (label only in column 0, or repeated into every column by a full-row merge) — a staff row with no header above it gets `roleName: ""`, surfaced downstream as "needs manual role assignment," never a borrowed/guessed label (the "COVERS" bug, now structurally impossible). (2) any parsed hour >=24 (the "hours past midnight" overnight notation, e.g. "26:00" or the real reference venue's literal decimal form "18.5") is always normalized by `% 24`, never passed through as an invalid literal (the overnight-hour bug, now structurally impossible). Also handles: the real reference venue's literal cell format (4 space-separated numbers = AM start/end, PM start/end, e.g. "11 17 18 25"), 2-number single-shift cells, hyphen/multi-segment single-cell ranges ("9-13", "10am/3pm-7pm/12am"), leave/absence codes (OFF/PH/AL/Sick/etc → `LeaveRecord`, not a shift or anomaly), and skips numeric-only first-cell rows (totals/headcount summary rows, a real garbage-row pattern seen in earlier live testing) instead of misreading them as staff names.
  - `server/src/parsing/deterministicGridParser.test.ts` — 7 unit/integration tests, including one exercising the REAL `buildMergeExpandedGrid()` pipeline against an actual `.xlsx` buffer with genuine `!merges` ranges (not a pre-expanded array), reproducing the exact shape already validated live against Gemini last session (11/11 correct). All 7 pass.
  - `server/src/routes/schedules.ts` router updated: Excel/CSV upload now tries (1) `parseWorkbookBuffer` — the 3 fixed long-format templates, unchanged — then (2) `parseExcelGrid` — the new deterministic day-grid parser, PRIMARY for this shape, zero network calls — then (3) `parseRosterGrid` (Gemini grid-text) only as a last-resort fallback when the deterministic parser doesn't recognize the shape at all (e.g. days-as-rows), preserving the format coverage already validated for those shapes rather than hard-rejecting. This is a deliberate deviation from the literal pivot spec (which routed all Excel/CSV to deterministic-only, no LLM branch) — kept the safety net since it was cheap (code already existed) and avoids regressing already-working coverage.
  - Verified: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), server tests (23/23, up from 16) all clean.
  - NOT yet done (explicitly deferred per the user's own sequencing — Excel first, validate, then PDF): (1) PDF positional table reconstruction via pdfjs-dist for text-layer PDFs (the real reference venue's actual file is a PDF, not Excel — this is the next piece needed before the pivot covers that file), (2) the router's PDF branch still uses the old text-extraction-then-Gemini-image path unchanged, (3) the Ollama/local-vision-model swap for the image/scanned-PDF LLM path, (4) the completeness safety-check + auto-retry on that Ollama path. All still Gemini-based / not started.
- [x] BUILT PDF positional table reconstruction (pdfjs-dist), completing the deterministic-primary pivot for the real reference-venue file (a PDF, not Excel). Checked first, as asked, whether the real PDF even has a text layer before writing any code: confirmed via both `pdf-parse` and `pdfjs-dist` (1722 chars extracted, real staff names + shift-number patterns present, 864 positioned text items on page 1) — genuinely a text-layer PDF, not scanned, so positional reconstruction is the right approach (Ollama vision would only be needed for a scanned/photographed roster, which this isn't).
  - New `server/src/parsing/pdfTableExtractor.ts` — `extractPdfGrid()`/`hasPdfTextLayer()`. Reads every text item's real x/y via `pdfjs-dist/legacy/build/pdf.mjs` (added as an explicit dependency; was previously only transitive via `pdf-parse`), row-clusters by y-proximity using a GAP from the previous row (not a fixed grid, so inconsistent row spacing doesn't break it), then derives column anchors from the header row's items specifically — NOT from every item on the page. That distinction mattered: a data row has far more positioned text items than visual columns (each raw number in a multi-value shift cell, e.g. "11 17 18 25", is its own item), so an initial attempt at clustering from all items fragmented one logical day-column into ~6 spurious ones (44 columns instead of ~8) and produced 0 usable rows. Anchoring on the header row's items alone fixed it in one pass — went from 0 rows to 160. A trailing anchor catches whatever sits right of the last day column (a notes/legend column on the real file) so that text doesn't bleed into the last day's cell and break its parsing. A continuation-line merge pass (single populated column + gap tighter than the page's typical row spacing) reassembles a role header wrapped across two lines. The reconstructed grid is fed into the exact same `parseExcelGrid()` already built for Excel — no second parsing engine, per the explicit instruction.
  - Fixing the real file's exact 21-employee layout surfaced two more real bugs in `deterministicGridParser.ts`'s role-header detection, both now fixed (shared engine, so Excel gets these fixes too): (1) the role-header rule assumed the label always sits in column 0; on this file the section labels (SUPERVISORS/HEAD WAITERS/WAITERS/RUNNERS) are visually centred and land in whichever middle day-column their x-position is closest to, so they were never being detected at all. (2) that same "row full of blanks + one label" heuristic was misfiring on real staff members who simply have zero shifts that week (on leave/PH the whole week, e.g. Sintia/Tomas/Irma/Tatenda/Rowel) — their own name was the "lone value," so it got used as the role for every employee listed after them. Fixed by reusing the existing `ROLE_ALIASES` vocabulary (`canonicalRoleName` from `resolveRows.ts`) as the disambiguating signal: a lone value only becomes the current role if it actually resolves to a known role keyword; a shift-less employee's own name doesn't, so it's correctly treated as a zero-shift staff row instead of a header, leaving `currentRole` untouched for whoever comes next. Also reordered role-keyword detection to run BEFORE the numeric-totals-row skip, since a real section-header row on this file happened to share its reconstructed line with an unrelated stray headcount number in column 0.
  - Result against the real file: **160 shift rows**, all 16 labeled/unlabeled-correctly employees matching the ground truth independently established during the earlier VLM investigation (Andrea/Roberto/Alessandro → `""`, correctly unresolved and flagged for manual review, never a guessed label; Pratik/Derrick → `SUPERVISORS`; Rojina/Rafael/Robert → `HEAD WAITERS`; Hefny → `WAITERS`; Bashkar/Ismail/Ranis/Mohamed/Oumaima/Subash/Zakir → `RUNNERS`), and exactly 2 anomalies — both the non-employee "COVERS" covers-count row's two unparseable annotation cells, correctly flagged rather than silently mishandled. 160 sits inside the live-Gemini post-fix baseline's tight cluster (162–170 across 5 runs, mean 167.6) — with the deterministic path there is now zero run-to-run variance at all, since no model call is involved. Known gap: the trailing notes/legend column's per-employee annotations (Holiday/Closing/Request/Sick/PH/Training/BDP) aren't interpreted as leave records the way the VLM path does — employees who are on leave the entire week (Sintia, Tomas, etc.) currently produce zero output rather than a `LeaveRecord`, since that data lives outside the 7 day columns this parser reads. Not a regression (those employees produced 0 shift rows either way) but a real feature gap versus the VLM path, worth a follow-up if leave visibility for all-leave weeks matters.
  - `server/src/parsing/pdfTableExtractor.test.ts` — 3 tests: (1) the real reference-venue PDF (`server/test-fixtures/real-roster.pdf`, already a committed fixture from an earlier session) through the full `extractPdfGrid` → `parseExcelGrid` pipeline, asserting the exact 160-row/role-by-employee/anomaly result above; (2) a synthetic PDF (built with `pdf-lib`, added as a devDependency) with a role header wrapped across two lines, verifying the continuation-merge reassembles it correctly; (3) a synthetic PDF with deliberately irregular row-to-row spacing (20pt/35pt/14pt gaps), verifying rows stay correctly separated rather than being spuriously merged. All 3 pass. The synthetic fixtures produce a benign pdfjs font-substitution console warning (pdf-lib's non-embedded Helvetica doesn't match pdfjs's fallback font file) — doesn't affect correctness, doesn't occur on the real PDF (which has an embedded font), not worth chasing further. Also fixed a similar warning on the real path by pointing pdfjs at its bundled `standard_fonts` directory via `standardFontDataUrl`.
  - `server/src/routes/schedules.ts` PDF branch restructured into the same layered fallback shape as Excel/CSV: `hasPdfTextLayer` check first (no positional reconstruction is attempted at all for a scanned/image-only PDF) → deterministic `extractPdfGrid`+`parseExcelGrid` as PRIMARY for a text-layer PDF, zero network calls → falls back to the existing line-oriented `parseRosterText` for a flat-list-shaped text PDF the grid reconstruction correctly declines to recognize → falls back to the existing Gemini vision path only as the last resort (scanned PDFs, or a layout neither deterministic path handles).
  - Verified: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), server tests (26/26, up from 23) all clean.
  - NOT yet done: the Ollama/local-vision-model swap for the remaining LLM path (still Gemini), and the completeness safety-check + auto-retry on that path — both explicitly out of scope for this step per the user's own sequencing.
- [x] ADDED leave-annotation capture to the shared deterministic grid parser (`deterministicGridParser.ts`, so both the Excel and PDF paths get it): when an employee has zero shift/leave data across every day column, any OTHER non-blank column on their row (a notes/comments column outside the day-date range) is checked for a known leave code (`LEAVE_CODES`, same vocabulary `parseCellValue` already uses per-cell); a match becomes an explicit `LeaveRecord` dated at `weekStart` instead of the employee silently vanishing with zero output. Deliberately narrow: only fires when the day columns were completely empty, so a real working week with unrelated trailing text (e.g. a "Closing" note next to someone who worked normally) is never misread as an absence — verified directly with a control case (`Andrea` worked + had a "Closing" note → 0 leave records; `Sintia`/`Tomas` had zero shifts + a leave-code note → 2 leave records; `Irma` had zero shifts + no note → correctly nothing).
  - Real-file finding worth knowing: the reference-venue PDF's trailing column that this feature reads from is actually a **static legend/key box** (11 fixed entries: Holiday/1920/Closing/Request/Horizon/Sick/Unpaid/PH/Training/BDP/Gatto Floor — independently confirmed against the legend array Gemini extracted from the same file in an earlier session), not genuine per-employee annotations — it only produces real leave records for Sintia ("PH") and Tomas ("Request") on this file because their zero-shift rows happen to coincide, row-for-row, with two of the legend's 11 lines. Even the Gemini vision path (reading actual cell colours, which text extraction can't see) produced 0 leave records for this exact file, meaning there was no confidently-visible leave marking on it either. Surfacing the coincidence anyway was a deliberate choice: a manager seeing "Sintia — PH" for review can dismiss a wrong guess in seconds, which is strictly better than the prior silent-zero-rows behavior, and on a real file where a manager genuinely writes "Sick" next to a blank row (the common case this feature targets) it's exactly correct.
  - `server/src/parsing/deterministicGridParser.test.ts` gained 1 test (control case above). `server/src/parsing/pdfTableExtractor.test.ts`'s real-PDF test updated to assert the exact `Sintia:PH` / `Tomas:Request` leave records, with the coincidence documented inline.
  - Verified: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), server tests (27/27, up from 26) all clean. No change to shift-row count (still 160) or anomaly count (still 2) on the real file — purely additive.
- [x] SWAPPED the Gemini vision last-resort fallback for a local Ollama vision model, closing the pivot's last item — the image/scanned-PDF path (now genuinely only reached when neither deterministic path applies) no longer depends on an external API at all. Checked local setup first: Ollama was installed and running (v0.32.14) but had no vision-capable model pulled (only `llama3`, `qwen2.5-coder`) — asked which one to pull given it's a real multi-GB download; picked **qwen2.5vl:7b** (~6GB) over `llava`/`llama3.2-vision` for being specifically strong at dense document/table/OCR-style reading, closer to this task than general-purpose captioning.
  - New `server/src/parsing/parseVisionOllama.ts` (`parseRosterImageOllama`) — POSTs to the local Ollama `/api/chat` endpoint (`OLLAMA_HOST` env, default `http://127.0.0.1:11434`; `OLLAMA_VLM_MODEL` env, default `qwen2.5vl:7b`), reusing the exact same `ROSTER_VLM_SYSTEM_PROMPT` and `mapVlmResponseToResult` mapping as the Gemini path — only the transport and the JSON-schema shape passed for structured output differ (Ollama's native `format` parameter takes a plain JSON Schema, so the previously-unused `ROSTER_VLM_JSON_SCHEMA` — originally written for OpenAI, dead code since the Gemini switch — became genuinely useful again instead of needing a third schema variant). Exported `VlmResponse` from `parseVision.ts` (was a local-only type) so this module could reuse it without duplicating the shape.
  - Completeness-retry check, as specified: a cheap, narrow "list only the staff names you see" call runs first as an independent baseline (much smaller output, far less likely to itself truncate than the full extraction) — if the full structured extraction is missing any name that baseline call found (checked across shift rows, anomalies, and leave records, not just rows), it retries the full extraction once, silently, before ever returning to the caller. Still-missing names after the retry are logged server-side (not blocking) so a manager reviewing the preview will still visibly notice whoever's absent.
  - `server/src/routes/schedules.ts` — both Gemini `parseRosterImage` call sites (the direct image-upload branch, and the PDF-fallback-to-vision branch) replaced with `parseRosterImageOllama`. Deliberately scoped narrow: `parseRosterGrid` (the Gemini **text**-mode fallback for an Excel/CSV grid shape the deterministic parser doesn't recognize, e.g. days-as-rows) was left on Gemini — the request specifically named "vision model" and "images/scans," and swapping a text-only call to a vision model doesn't fit; flagged this scoping choice rather than assuming. No Gemini fallback-of-the-fallback was added if Ollama itself fails (not running / model not pulled) — surfaces as a clear `VisionIngestionError` instead, per "I don't want an API dependency even for the rare fallback case" (a silent fallback to Gemini would have reintroduced exactly that dependency).
  - `server/src/parsing/parseVisionOllama.test.ts` — 2 mocked tests (via `ollamaHttp.fetch`, see below) verifying the retry logic deterministically: retries exactly once and returns the complete result when a name is missing; does NOT retry when nothing is missing. Both pass.
  - LIVE-TESTED against the real reference-venue roster image (`server/test-fixtures/real-roster.png`) once qwen2.5vl:7b finished pulling, and hit two real bugs in the process (both fixed): (1) **400 "exceeds context size"** — Ollama defaults to a 4096-token context window regardless of what the model supports unless explicitly overridden per-request; the real prompt+schema+image needed ~5973. Fixed by setting `options.num_ctx` (env `OLLAMA_NUM_CTX`, default 16384) on every call. (2) **A premature timeout that fired independently of the code's own AbortController** — Node's built-in global `fetch` (undici under the hood) has its own internal `headersTimeout` defaulting to ~300s that killed the request at ~357s even with a 600s AbortController configured. Fixed by routing through the `undici` npm package directly (added as an explicit dependency) with a custom `Agent` whose `headersTimeout`/`bodyTimeout` actually match `OLLAMA_TIMEOUT_MS` — exposed as an exported `ollamaHttp = { fetch }` object specifically so the existing mocked tests could keep mocking a stable target instead of the ambiguous global.
  - Real-hardware finding, discussed and a decision made rather than assumed: even after both fixes and raising the timeout to 10 minutes, the full structured extraction on this specific 21-employee/160+-shift reference image still hadn't completed on this machine (RTX 4050, 6GB VRAM — the 6.9GB model doesn't fully fit, so Ollama runs a ~50/50 CPU/GPU split, and CPU-assisted generation of a large structured JSON response is slow). Decision: **ship as-is** — this is the rare last-resort path (Excel and text-layer PDFs never reach it after the earlier pivot work), the reference file is an unusually dense stress-test case, and staying fully local with no API dependency was the explicit point of this swap. Real photo uploads from smaller venues are expected to be lighter and may well complete comfortably, but that's unverified until real usage confirms it.
  - Given that finding, the live integration test is now gated behind `RUN_OLLAMA_LIVE_TEST=1` (skipped by default with a clear reason) rather than running — and potentially failing on slow hardware — as part of the normal fast test suite; run it manually when checking actual throughput on a given machine/model.
  - Verified: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), server tests (29/30 — the live test correctly self-skipped by design, not a failure) all clean, full suite runs in ~4s (not 10+ minutes).
- [x] GENERALIZED role/section-header detection in `deterministicGridParser.ts` — it was entirely vocabulary-coupled (`canonicalRoleName`/`ROLE_ALIASES`), meaning any venue's own section wording not already in that hospitality-role word list (e.g. "FOH TEAM", "BAR CREW") would never be recognized as a header at all and its staff would just fall through as unlabeled. Added a second, pattern-based signal: `isRoleHeaderLabel()` now also recognizes a lone ALL-CAPS label as a header **once at least one real staff row has already been seen** in the parse — the position gate is what tells a genuine role-section header (which always sits inside the staff listing, splitting one group from the next) apart from some other all-caps sheet caption before the listing starts (a covers-count panel title, a legend heading) that has nothing to do with role grouping. First attempt without that position gate immediately regressed the original "COVERS" bug (an unrelated all-caps caption at the top of the real reference file got treated as a header again) and broke ALL-CAPS leave codes ("OFF", "UNPAID") colliding with the new pattern — both caught by re-running the existing test suite, not just the new tests, and fixed: leave codes are now checked and excluded first, unconditionally.
  - 2 new tests added: a synthetic grid using section labels genuinely absent from `ROLE_ALIASES` ("FOH TEAM", "BAR CREW") confirms the generalization actually extends coverage, not just re-detecting words already in the vocabulary; a second reproduces the "COVERS"-before-any-staff-row shape directly to guard against the exact regression that surfaced during development. All 10 grid-parser tests + all 3 PDF-extractor tests (including the real reference file, still exactly 160 rows/correct roles) pass with zero change to existing behavior on Gattopardo — the generalization is additive, invisible on a file whose vocabulary was already fully covered.
  - **Honest coverage picture, as asked for** — what's still NOT handled after this pass: (1) a venue that writes every staff name in the sheet in capital letters (a style choice, not a header signal) will still misfire, same failure class as a coincidental vocabulary match before, just a different trigger; (2) a genuinely novel-vocabulary section header that is the very first labelled group in the sheet, with no unlabeled staff rows above it — the position-gate requires at least one prior staff row, so a bare pattern match won't catch that specific case (only the vocabulary-match path would, if the word happens to be a known one); (3) **not yet validated against a second real file** — the user's own explicit ask was "add Bar des Pres's actual file as a second permanent fixture and get both passing," and that file wasn't available locally (searched Downloads and the user profile, not found) — asked the user to provide it; this generalization is evidence-based only against the one real file already in the repo (Gattopardo) plus hand-built synthetic cases, not yet against a second independently-sourced real-world layout. Do not describe this as "now handles any file" until that validation happens.
  - Also **not addressed in this pass** (out of scope, not attempted): the header-row-detection side (`findHeaderRows`) still assumes the AM/PM sub-header row sits immediately below the day-name row with nothing in between, and still only recognizes one overall header shape (day-columns, optional AM/PM row) — a file with a materially different header structure (e.g. a spacer/venue-name row between the date row and the AM/PM row, or no AM/PM row concept at all) has not been tested and may not be recognized. Role-header detection was the specific, scoped ask; broader header-shape generalization is a separate, larger piece of work.
- [x] REMOVED the paste-text parser tab from the UI entirely, per explicit request — only file upload (Excel/CSV/PDF/image) remains. `src/App.tsx`: removed the `parseRosterText` call, the `text`/`setText` state, the `SAMPLE` constant, the "Parser" `<textarea>` section, and the now-always-empty "Warnings"/"Unparsed lines" sections that only ever surfaced issues from that removed text parser. `roster` (the base `mergedRoster` starts from before any upload) is now a small locally-constructed empty shell (`{id, venueId, weekStart, employees: [], shifts: [], createdAt}`) instead of the text-parser's output — `Dashboard` still needs a valid `weekStart` for its date-range display, which this preserves. Cleaned up the now-fully-unused `.parser`/`.warnings`/`.paste-area` CSS rules from `global.css` (`.roster` and `.hint`, which the removed sections shared class names/groupings with, are still used elsewhere and were kept). Note: `src/engine/parser.ts` (the underlying text-parsing engine) and its own test suite (`src/engine/parser.test.ts`) were NOT touched — only the App.tsx UI wiring that called it was removed; the engine module itself still exists and is still unit-tested, just no longer reachable from the UI.
  - Verified: `npm run typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11) all clean.
- [x] ADDED Bar des Pres as the second permanent fixture and closed 4 real format gaps it surfaced, plus 1 more found while building the reference test. Real file provided by the user ("April 13-19.pdf") — checked first (as established practice this session) whether it has a text layer: **it does not** (0 positioned text items via pdfjs-dist; pdf-parse extracted 16 characters of pure boilerplate, no roster content) — almost certainly an Excel sheet exported/printed as a flat image, structurally unlike Gattopardo's genuine text/vector PDF. This means it can't run through `extractPdfGrid`/`hasPdfTextLayer` the way Gattopardo does — no automated "real file → grid" extraction test exists for it. Per the user's explicit direction, added it anyway as a **hand-transcribed documented reference fixture** (`server/src/parsing/barDesPresReference.test.ts`) — the real, independently-sourced content read directly off the rendered image, testing the interpretation side (`parseExcelGrid`) against real complexity rather than synthetic approximations. 5 tests, all pass.
  - **(1) Per-row title column.** This file gives an explicit title per person (RM, AM, JAM, Supervisor, Head waiter 1-5, Waiter 1-4, Runner 1-5, Chef de pass) in its own column before the name — a structurally different shape from Gattopardo (role only ever via section header). `parseExcelGrid` now infers a title column from where the day columns actually start (`firstDayColIndex >= 2` → column 0 is a per-row title, the column just before the first day column is the name) rather than assuming column 0 is always the name — general by position, not hardcoded to this venue. Per-row title takes precedence over section-header-derived role when present, falls back to it when blank. Required a genuine restructure of the row-classification logic (single-title-column and two-title-column shapes discriminate "is this a header row?" differently — content-based vs. position-based) with a shared `processStaffRow` inner closure so both paths reuse the same per-cell logic instead of duplicating it. Re-ran the full existing suite (not just new tests) after the restructure specifically because the first version silently regressed the earlier PDF-reconstruction stray-number fix; caught and fixed before it shipped.
  - **(2) "UL" added to `LEAVE_CODES`** as Unpaid Leave, confirmed with the venue.
  - **(3) Shorthand codes** ("10IN", "12CL", bare "IN"), exact meanings confirmed with the venue: open-ended (real start, no end), until-closing (real start, ends at an unspecified closing time), and fully flexible (no fixed start or end) respectively. **Scoped down from the literal ask**: requested as "not an anomaly," implemented as a distinct `CellParseResult` kind (`'flagged'`) routed through the *existing* `anomalies` array with a clear structured reason and higher confidence (0.7, vs. 0.2 for genuinely unparseable garbage) — signalling "understood, needs a decision" rather than "couldn't read this." A true separate "open-ended shift" concept in the DB/UI would need `ParsedShiftRow.startTime`/`endTime` to stop being both-required (`Shift.endTime` is a required DateTime column), which is a real schema change across the DB, persistence, and UI layers — out of scope for a parser-generalization pass. Flagged this deviation explicitly rather than silently reinterpreting the ask.
  - **(4) Hyphen-chained 4-number split shifts** ("10:30-4:00-8:00-12", no slash) now parse as two back-to-back shifts, distinct from the existing slash-separated multi-segment form. Bare numbers are read as literal 24h hours, consistent with every other numeric cell this parser already handles — for this specific example that means "4:00" reads as 04:00, not the almost-certainly-intended 16:00 (10:30am-4:00pm is a far more natural lunch shift than a 10:30pm-4:00am one). Not fixed with an unverified AM/PM-guessing heuristic; documented as an open interpretation risk in both the code and the reference fixture rather than silently guessed.
  - **(5, found building the reference test, not part of the original 4-item ask):** this file stacks *two* header rows — numeric dates ("13-Apr") then weekday names ("Monday") directly below, with no AM/PM row at all. `findHeaderRows` only knew how to skip past an AM/PM row; without a fix the weekday-name row would have been mistaken for the first staff row. Fixed generally: after the date row, if the next row isn't an AM/PM row, check whether it independently resolves >=2 more dates itself (same test the date row passed) and skip it too before data starts.
  - **Real findings surfaced by testing against actual messy data, not fixed in this pass:** an employee whose entire transcribed week is leave codes and/or flagged shorthand (Robert Orgovan/RM, Shovit Shrestha/AM, George Karanja/Chef de pass — all real, all in the reference file) ends up with **zero** `ParsedShiftRow` entries, and their role is recorded nowhere in the output at all — `AnomalyRecord` and `LeaveRecord` both carry no role field. Their name still appears (in anomalies/leave records), but a manager reviewing the preview has no way to see what their job title even is unless they also show up with a real shift some other week. Not fixed here — flagged for a decision on whether it's worth threading `roleName` through both types.
  - Full suite re-verified after all 5 changes: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, frontend tests (11/11), server tests (41/42 — the 1 skip is the gated Ollama live test, not a failure, up from 27) all clean. Gattopardo's real-file test unchanged throughout at exactly 160 rows/correct roles — every Bar des Pres-driven change was additive there too.
  - **Honest overall coverage picture, as explicitly asked for — do not describe this as "handles any roster file":** validated against exactly 2 real files (Gattopardo: genuine text-layer PDF, day-columns-only, section-headers-only; Bar des Pres: image-only PDF tested via hand transcription only, per-row-titles-plus-section-headers). Known NOT handled: (a) a venue that writes every staff name in ALL CAPS (style choice, not a header signal) will misfire the role-header pattern; (b) a novel-vocabulary section header that's the very first labelled group with no unlabeled staff above it won't be caught by the pattern signal; (c) the header-shape detection still only recognizes "day-columns, optional single AM/PM row, optional single stacked weekday-name row" — a materially different header structure hasn't been tested; (d) the AM/PM ambiguity in bare hyphen-chained numbers is unresolved, documented not fixed; (e) role is invisible for an employee whose whole week is leave/flagged codes.
- [x] BUILT the Staff Directory + Management tier — the last piece of the original 3-item ask (numbered #1/#2 in the request, done last per the user's own priority order). Directly closes the exact gap the parser generalization work exposed: Andrea/Roberto/Tomas/Alessandro (Gattopardo's unlabeled top rows) correctly get `roleName: ""` from an upload — the parser declining to guess is the *right* behavior — but the frontend grid previously had no way to know they're actually management, so they fell into "Other." The Staff Directory lets a venue say so once, manually, and the grid now trusts that over whatever (if anything) a given week's upload resolved.
  - **Backend, persisted (not frontend-only/localStorage):** staff already exist as `User` records per `Location` with a real Postgres-backed DB, so this extends that rather than inventing a parallel store. `prisma/schema.prisma`: added `User.jobTitle String?` (distinct from `role`/`Role`, which is the shift-scheduling category like Waiter/Runner — `jobTitle` is the venue's own specific title, e.g. "Restaurant Manager" vs "Assistant Restaurant Manager"). Migration `add_user_job_title` applied to the live Supabase DB. New `server/src/routes/staffDirectory.ts`: `GET /api/staff-directory/:locationId` (list), `POST /api/staff-directory` (add), `PATCH /api/staff-directory/:userId` (edit name/title) — no DELETE endpoint, kept deliberately out of scope for "simple." Mounted in `app.ts`.
  - **Frontend:** `src/api/staffDirectory.ts` (client, mirrors `schedules.ts`'s conventions) and `src/components/StaffDirectory.tsx` — a collapsible panel below Upload Roster: a table of current staff with an inline-editable job-title field (saves on blur) plus a small add-staff-member form. Matches the existing dark-luxury design system (`.preview-table`/`.btn`/`.hint` classes reused; a handful of new `.staff-directory-*` rules added to `global.css`).
  - **Management tier:** `App.tsx`'s existing `ROLE_SECTIONS` already had a `manager` tier positioned first (above Supervisor) — relabeled "Manager" → "Management" to match the ask, and added `sectionForEmployee()`, which checks the Staff Directory FIRST (by exact case-insensitive name match) before falling back to the existing parsed-role matching. Management-title detection is pattern-based (`/manager|management/i` on the job title), not a fixed word list — deliberately consistent with this session's broader "pattern over hardcoded vocabulary" direction, so it works for whatever specific title wording a venue actually uses without needing every variant enumerated.
  - **Verified:** `npm run typecheck`, `npm run build`, `npm run lint`, full frontend+server test suites (11/11 + 41/42, no regressions) all clean. Live-tested the backend against the real Supabase DB (not just typechecked): `GET` returned the real seeded Gattopardo staff, `POST` created a test entry, `PATCH` updated its job title and a follow-up independent `GET` confirmed the change actually persisted (not just echoed back), then the test record was deleted via a direct Prisma call to avoid leaving fake data in the DB, and the dev server was stopped afterward. **Not visually verified** — no browser/screenshot tool available (per the project's own established gotcha); the panel's actual rendering, collapse/expand behavior, and inline-edit UX have not been seen, only reasoned through against the existing component patterns.
  - **Known scope/precision limits, stated plainly:** (1) name matching between the roster grid and the Staff Directory is exact-normalized-string (lowercase+trim) — "Andrea" in an upload must match "Andrea" (or the venue must enter it that way) in the directory; no fuzzy matching. (2) No delete/deactivate UI for a directory entry. (3) The Management-tier cross-reference only affects grid *grouping* — it doesn't change what role gets persisted to the `Shift` record in the DB (that's still whatever `resolveRowsAgainstDatabase` resolved at confirm time), so a manager pulling data directly from Postgres would still see the upload's own resolved role, not the Staff Directory's.
- [x] PORTED the global chrome layer (sticky glass header + circular radial bottom-nav) across the whole app, replacing the old plain `.app-header`/`.brand`/`.tagline` header and flat `.bottom-nav` row, via a 7-task plan (`.superpowers/sdd/2026-08-25-global-chrome-grouping/`), each task built and reviewed independently:
  - **Task 1** — extracted a shared role-grouping module, `src/engine/roleGrouping.ts` (`roleKey`, `sectionForRole`, `isManagementTitle`, `sectionForEmployee`, `groupIntoSections`, `ROLE_SECTIONS`: Management→Supervisor→Head Waiter→Waiter→Runner→Other), covered by `src/engine/roleGrouping.test.ts` — no per-screen duplication of this logic going forward.
  - **Task 2** — `src/state/AppStateContext.tsx`, a shared app-state context exposing `sections: GroupedSection<E>[]` (built on Task 1's exact return type) plus roster state, consumed by the new routes instead of prop-drilling from `App.tsx`.
  - **Task 3** — real client-side routing added via `react-router-dom` (flagged as an explicit architectural choice, not spec-mandated — `@tanstack/react-router` was rejected as needing a codegen/SSR build step this project doesn't have). Routes: `/` (`HomeRoute.tsx`), `/scheduling` (`SchedulingRoute.tsx`, still owns `.roster`/`.section-title`/`.roster-table-grid`/`.grid-*`/`.cell*`/`.roster-section*`/`.badge*` CSS), `/floor-plan` (`FloorPlanRoute.tsx`), `/people` (`PeopleRoute.tsx`), `/profile` (`ProfileRoute.tsx`, a stub).
  - **Task 4** — new Tailwind utility classes added for the chrome layer, tokens kept pinned to the existing `global.css`/dark-luxury palette (no second palette introduced).
  - **Task 5** — `src/components/shiftsync/AppShell.tsx`: sticky glass header (gold-bordered "S" avatar linking to `/profile`, glowing gold notification bell), applies `pb-24` directly via Tailwind on its own root `<div>` (replaces the old `.app-shell { padding-bottom: 5rem }` CSS rule).
  - **Task 6** — `src/components/shiftsync/RadialDock.tsx`: circular bottom-nav dock, 4 tabs (Home/Scheduling/Floor Plan/People) + floating gold voice-toggle button (stub pulse animation only, no real voice backend — expected per build directive), tab paths matching the router config exactly.
  - **Task 7 (this entry)** — retired the now-dead CSS: removed `.app-shell`, `.app-header`, `.brand`, `.tagline`, and the full `/* Bottom nav */` block (`.bottom-nav`, `.bottom-nav-item`, `.bottom-nav-item-active`) from `src/styles/global.css`, after confirming via `grep -rn "app-shell\|app-header\|\"brand\"\|bottom-nav" src --include=*.tsx` that no `.tsx` file referenced any of them post-Tasks 3/5/6. Kept `.roster`/`.section-title`/`.roster-table-grid`/`.grid-*`/`.cell*`/`.roster-section*`/`.badge*` untouched (still used by `SchedulingRoute.tsx`). Full verification pass all green: `npm run typecheck`, `npm run lint` (0 errors, 1 pre-existing unrelated warning in `AppStateContext.tsx`), `npm run build`, `npm test` (20/20, including `roleGrouping.test.ts`). Server-level check only (no browser/screenshot tool in this environment, per established project convention): `npm run dev:all` booted cleanly (Vite ready, API listening on :4000), and `/`, `/scheduling`, `/floor-plan`, `/people`, `/profile` all returned HTTP 200 with the real Vite dev HTML shell — proves the router doesn't 404/crash server-side on any route. **Not visually verified**: the sticky header, gold avatar/bell, radial dock's 4 icons + floating voice button, drag/tap navigation, and the "listening" pulse animation have not been seen rendered — that requires a human looking at a real browser and is still outstanding.

## Next Sprint Goals
- Real client-side routing now exists (`react-router-dom`; routes at `/`, `/scheduling`, `/floor-plan`, `/people`, `/profile` — see `src/routes/*.tsx`). Future phases needing an addressable URL (Join's invite link, onboarding-wizard steps, deep-linkable shift/section views) should add a route here rather than reintroducing a single `activeTab` string.
- A shared role-grouping module now exists at `src/engine/roleGrouping.ts` (`groupIntoSections`, `sectionForEmployee`, `ROLE_SECTIONS`, etc.) — the next phases that need role-based sectioning (RotaBuilder, Team Matrix) should import from here rather than re-deriving Management→Supervisor→Head Waiter→Waiter→Runner→Other grouping logic per-screen.
- Outstanding from the chrome-layer phase above: a human visual/interactive pass in a real browser against the new `AppShell`/`RadialDock` chrome (sticky header, avatar/bell, radial dock nav + voice-toggle stub animation) — no screenshot/browser tool available in this environment to do it automatically.
- [ ] Build a double-pass reliability cross-check for the VLM path (fire two Gemini calls, diff per-employee/row counts, surface mismatches for review instead of trusting one pass). Raised to higher priority: the stress-test runs above show the model occasionally drops an entire employee/row-group on an otherwise-clean call (2/3 pass rate on 2 of 3 synthetic formats), independent of input structure and independent of the overnight-hour schema fix — this is a residual reliability gap the schema pattern fix does not close, since it's not a formatting error the schema can forbid, just incomplete generation.
- [ ] Revisit local Ollama vision performance once real usage data exists. The dense 21-employee reference file didn't complete in 10 minutes on this machine's 6GB-VRAM GPU (partial CPU offload) — accepted as a stress-test outlier for now, but if real photo uploads (smaller venues) also turn out to be too slow in practice, worth trying: a smaller/faster model (moondream, a smaller qwen2.5vl quantization), or a leaner extraction schema for this path specifically (the per-cell schema asks for confidence/reviewReason/leaveCode on every cell, which is a lot of output to generate for a large roster).
- [ ] Decide whether to thread `roleName` through `AnomalyRecord`/`LeaveRecord` (currently neither carries it). Found via the Bar des Pres reference fixture: an employee whose entire week is leave codes and/or flagged shorthand (real examples: Robert Orgovan/RM, Shovit Shrestha/AM, George Karanja/Chef de pass) ends up with zero `ParsedShiftRow` entries, so their job title is recorded nowhere in the parsed output at all — a manager reviewing the preview sees their name with no role, every week they don't happen to have a real confirmed shift.
- [ ] Decide the real design for open-ended/until-closing/flexible shifts ("10IN"/"12CL"/"IN" — confirmed Bar des Pres shorthand). Currently routed through `anomalies` (a scoped-down interpretation of the original ask, which wanted a distinct non-anomaly treatment) because `ParsedShiftRow.startTime`/`endTime` are both required and `Shift.endTime` is a required DB column — a proper fix needs those to become optional/nullable across the shared type, persistence layer, and UI, not just the parser.
- [ ] Resolve the AM/PM ambiguity in bare hyphen-chained split-shift numbers ("10:30-4:00-8:00-12"). Currently read as literal 24h hours for consistency with every other numeric cell this parser handles, but for this real example that's almost certainly wrong (10:30am-4:00pm is a far more natural shift than 10:30pm-4:00am) — needs either venue-confirmed disambiguation rules or a lower-confidence anomaly flag when a hyphen-chain result looks implausible (e.g. an 18+ hour shift).
- [ ] **Multi-outlet single-file rosters** — a real, named pattern (7shifts sells a "Multi-Location Restaurant Schedule Template"). `deterministicGridParser.ts` only reads the first outlet block in a file; a second/third outlet's own title row and re-declared day-header get misread as staff-row garbage instead of being recognized as a new section. Found via live gap-report testing, not yet fixed.
- [ ] **Legend-code shift tables** (single-letter codes like M/E/N/G with actual hours defined in a separate legend table, not the cell — a documented real hotel/F&B duty-roster convention per setupmyhotel.com). The main grid's codes are correctly flagged as unresolved (never-guess principle holds), but the legend table itself gets misread as a second, phantom shift grid. No code exists yet that recognizes "this block is a legend, not staff data."
- [ ] **Dubai fine-dining role vocabulary gap** — confirmed via direct grep, not inference: real Dubai brigade titles (Outlet Manager, Captain, Chef de Rang, Demi-Chef de Rang, Commis de Salle, Chef de Partie, Commis Chef) are not in `ROLE_ALIASES` at all. Any of these resolves to `unmatched_role` even when the column/row structure is read correctly. Straightforward to close (add aliases) — deliberately not done as part of the column-swap fix, which was scoped to structural disambiguation only, not vocabulary.
- [ ] **Bilingual (Arabic/English) and days-as-rows uploads bypass the local-first architecture entirely** — both defeat `deterministicGridParser.ts`'s header detection and fall through to a live Gemini call (`parseRosterGrid`, Excel/CSV's own pre-existing text-mode fallback — a 4th tier, distinct from Docling/Ollama, not previously accounted for when "the three-tier pipeline" was described). Needs a decision: extend local-first coverage to these two shapes, or accept the occasional cloud-API cost for them as a deliberate tradeoff.
- [ ] **No `GET` route exists anywhere in `server/src/routes/`** — confirmed via grep, zero results. The entire backend is write-only from the frontend's perspective; `mergedRoster.shifts` in `App.tsx` is populated only by the one-time post-confirm `handleCommitted` push, never re-fetched from the DB. A page reload loses the rendered grid even though the `Shift` rows are genuinely still in Postgres. Found while reconciling the parsing backend against a real Scheduling/Weekly Rota Builder view (see the investigation entry below) — needed before any *reloadable* schedule view (as opposed to the current session-only grid) can exist. Proposed direction (not built): `GET /api/schedules?locationId=&weekStart=`, reading `Shift` joined with `Role`/`User`, scoped to a week.
- [x] Manager-facing preview/confirm UI for Feature 9 built: `src/components/ShiftUpload.tsx` (drag-drop upload → parse → review table → confirm import) wired into `App.tsx`, backed by `src/api/schedules.ts` client (mirrors server preview-row contract) and a Vite `/api` proxy to `localhost:4000`. Uses the existing dark-luxury CSS-variable design system (no Tailwind — project has none installed; kept consistent with codebase). Verified via `npm run typecheck` and `npm run build` (both clean). Live smoke-tested end-to-end against the running `server:dev` + seeded Supabase DB: uploaded `server/test-fixtures/sample-roster.xlsx` → preview returned 3 rows (2 matched, 1 `new_employee`, 1 parse issue for a missing name, overnight shift detected) → confirm committed 3 shifts → test shifts deleted afterward, server stopped, port 4000 closed. `App.tsx` passes `locationId="seed-location"` (matches the seeded location).
- [x] PDF ingestion added to the upload pipeline. Installed `pdf-parse` (v2.4.5, ESM). New `server/src/parsing/parseText.ts` — a text-block roster parser that mirrors the frontend engine's line strategy (day-name headers, "Name role 5pm-1am Fri" shift lines, role-label splitting) but emits the server's canonical `ParsedShiftRow[]` contract, reusing server helpers (`parseTimeCell`, `isOvernight`) and resolving day names against a `weekStart` (defaults to current week's Sunday via `currentWeekStart()`). `server/src/routes/schedules.ts` now branches on file type: PDF → `PDFParse.getText()` → `parseRosterText`; Excel/CSV → `parseWorkbookBuffer`; both flow through the identical `resolveRowsAgainstDatabase` → preview → confirm pipeline. Multer filter + frontend dropzone `accept`/subtitle updated to include `.pdf`. Verified: `server:typecheck`, root `typecheck`, `build` all clean; live smoke-tested a text PDF (`server/test-fixtures/sample-roster.pdf`) → preview returned 3 rows (2 matched, 1 `new_employee`, overnight detected, day names resolved correctly against `weekStart`) → confirm committed 3 shifts → test shifts deleted, server stopped, port 4000 closed. Note: scanned/image-only PDFs with no OCR text layer return a 422 ("No extractable text found") — OCR is a future enhancement.
- [x] VLM (Vision-Language Model) ingestion path added for arbitrary/unhardcoded roster layouts (image snapshots, colour-coded Excel-as-image grids, WhatsApp screenshots). `server/src/parsing/vlmPrompt.ts` — adaptable system prompt instructing the model to spatially re-derive any grid from scratch each time (orientation, merged/stacked headers, name+role columns, per-venue shift-code legend) with a strict JSON schema covering `worked_shift`/`leave`/`day_off`/`public_holiday`/`unresolved` interpretations plus a per-cell `confidence`/`needsReview`/`reviewReason`. `server/src/parsing/parseVision.ts` — calls the vision-capable model, then maps the response into the existing canonical `ParsedShiftRow[]`/`RowIssue[]` contracts plus two new types added to `server/src/parsing/types.ts`: `AnomalyRecord` (unresolved cells/codes/dates — metadata anomaly fallback) and `LeaveRecord` (AL/PH/DO-style non-working days, not persisted as shifts). Anything below a `0.6` confidence floor, dated with an unresolved calendar date, or explicitly `unresolved`/flagged by the model is routed to `anomalies` instead of being guessed. `server/src/routes/schedules.ts` now branches image uploads (`.png/.jpg/.webp/.gif`) straight to `parseRosterImage`, bypassing the 3 fixed master templates entirely, and threads `anomalies`/`leaveRecords`/`legend` through the existing preview response (a zero-workable-row image upload is no longer a hard 422 if it yielded anomalies/leave data). `src/api/schedules.ts` and `src/components/ShiftUpload.tsx` updated to accept image files and render a new gold-accented "Needs manager review" anomaly panel plus non-working-day and inferred-legend panels ahead of the existing preview table (`.anomaly-block` added to `src/styles/global.css`, matching the dark-luxury `--accent`/`--surface-2` tokens). Verified via `npm run server:typecheck`, root `typecheck`, `build`, and `lint` — all clean.
- [x] VLM backend switched from OpenAI to Google Gemini. `server/src/parsing/parseVision.ts` now uses the `@google/genai` SDK (`GoogleGenAI` client) with the `GEMINI_API_KEY` env var (default model `gemini-2.5-flash`, overridable via `VLM_MODEL`), sending the image as `inlineData` base64 and requesting structured output via `responseMimeType: 'application/json'` + `responseSchema`. Added `ROSTER_VLM_GEMINI_SCHEMA` to `server/src/parsing/vlmPrompt.ts` — a Google `Schema`-shape (Type enum, `properties`/`required`/`items`/`enum`) equivalent of the old OpenAI `json_schema` contract. `server/src/routes/schedules.ts` PDF branch now falls through to `parseRosterImage` when a PDF has no extractable text layer (scanned/image-only PDFs are handed to Gemini directly instead of returning a 422). `.env.example` updated to `GEMINI_API_KEY`/`VLM_MODEL="gemini-2.5-flash"`; removed the now-unused `openai` dependency from package.json. Verified via `npm run server:typecheck` (clean). NOT yet live-tested against the real Gemini API or a real roster image/screenshot (no `GEMINI_API_KEY` configured in this environment) — the Gemini call path, `responseSchema` compliance, and end-to-end preview rendering with real anomaly data are unverified until credentials are available.
- [x] Hardened the Gemini vision error path (was surfacing as a generic 500). Verified the `@google/genai` payload is correct: `inlineData: { mimeType, data: base64 }` is the proper Part structure (confirmed by direct SDK call — it reaches the API-key check, so no serialization/validation error), `gemini-2.5-flash` is a valid free-tier model, and `responseSchema` (a Google `Schema`-shape object) is accepted by the SDK's `processJsonSchema` transformer. Fixed the error handling so failures are diagnosable instead of a bare 500: `VisionIngestionError` now accepts an optional `cause` and appends the original error's stack; `parseRosterImage` logs the full error (`console.error('[parseVision] Gemini request failed', err)`) before rethrowing, and wraps `mapVlmResponseToResult` in its own try/catch (a mapping runtime error previously escaped as a non-`VisionIngestionError` → 500); the route's outer catch in `schedules.ts` now also logs `err.stack`. Reproduced the flow against a running server: an invalid/missing key returns a clean 422 with the full `ApiError` stack logged to stderr (`server-dev.err.log`), not a 500. Verified via `npm run server:typecheck`, root `typecheck`, and `build` (all clean).
- [x] Made the Gemini vision mapping lenient so valid shifts with unconventional formatting are accepted instead of the whole file being rejected ("No valid shift rows could be parsed"). Root cause: `mapVlmResponseToResult` used strict regexes (`ISO_DATE_RE` `^\d{4}-\d{2}-\d{2}$`, `HHMM_RE` `^\d{2}:\d{2}$`) and a strict `interpretation` enum, so a cell like `14/07/2026` / `9:00` / `"worked shift"` was dropped; when every cell dropped, all three output arrays were empty and the route's 422 guard fired. Fixes in `server/src/parsing/parseVision.ts`: (1) dates/times now go through the same lenient `parseDateCell`/`parseTimeCell` helpers the Excel/PDF paths use (accepts `DD/MM/YYYY`, `D/M/YYYY`, `9:00`, `17:00:00`, etc., normalized to ISO `YYYY-MM-DD` / `HH:mm`); (2) added `normalizeInterpretation()` mapping variations (`"worked shift"`, `"annual leave"`, `"dayoff"`, `"public holiday"`, `"AL"`, `"DO"`, `"PH"`, etc.) to the canonical enum; (3) lowered `REVIEW_CONFIDENCE_THRESHOLD` from 0.6 → 0.4 so a resolvable shift with a slightly unusual code is still surfaced to the manager rather than dropped; (4) `server/src/routes/schedules.ts` 422 guard now also requires `parsed.issues.length === 0` before hard-rejecting (a sheet that yielded only parse issues is no longer a hard error). Also added `console.log('[parseVision] raw Gemini JSON:', raw)` so the model's actual output structure is inspectable in the server log. Verified via a `mapVlmResponseToResult` unit run: `14/07/2026`+`9:00`+`worked_shift` and `2026-07-15`+`"worked shift"` both become rows; `X2` unresolved and a 0.30-confidence cell route to `anomalies`; `"annual leave"` becomes a leave record. `npm run server:typecheck`, root `typecheck`, and `build` all clean.
- [x] LIVE-TESTED the Gemini vision path end-to-end against a real restaurant roster PDF ("Gattopardo rota 17-23 Aug.pdf", copied to `server/test-fixtures/real-roster.pdf`). This surfaced and fixed three real issues: (1) the `.env` `GEMINI_API_KEY` had a stray leading `"` quote (value was `"AQ.Ab8R...`) that made the server send an invalid key → fixed the `.env` entry; (2) the model `gemini-2.5-flash` is deprecated for new users (Google returns 404 "no longer available to new users... use gemini-3.6-flash") → updated the default in `parseVision.ts` and `.env.example` to `gemini-3.6-flash`; (3) the real roster is a grid/table layout (columns of dates, rows of shift codes like `11 17 18 25`) that the line-oriented text parser can't handle, so the PDF branch in `schedules.ts` now falls through to the Gemini vision path whenever the text parser yields zero rows (not just when the PDF has no text layer). Also added `resolveDayMonthDate()` in `server/src/parsing/normalize.ts` to resolve Gemini's year-less `"18-Aug"` / `"17-Aug MONDAY"` dates to full ISO dates against the roster `weekStart` (passed through `parseRosterImage` → `mapVlmResponseToResult`). Result: the real roster upload returns HTTP 200 with a `batchId` and 86 parsed shift rows (employees, roles, dates, start/end times, overnight flags, manager-review notes), with unresolvable cells routed to `anomalies`. Verified via `npm run server:typecheck`, root `typecheck`, and `build` (all clean).
- [x] Added `server/scripts/seed-gattopardo.ts` — seeds `seed-location` with the real Gattopardo roster's staff and roles so roster uploads resolve to `matchedRows` in the browser UI. Because Gemini's role extraction is non-deterministic across runs (it emits "Management", "Management / Floor", "Staff", "Supervisor", "SUPERVISORS", "Head Waiter", "HEAD WAITERS", "Waiter", "WAITERS", "Runner", "RUNNERS"), the script seeds every observed role label plus all 21 staff members (Andrea, Roberto, Tomas, Alessandro, Sintia, Pratik, Derrick, Rojina, Rafael, Irma, Robert, Hefny, Tatenda, Bashkar, Ismail, Ranis, Rowel, Mohamed, Oumaima, Subash, Zakir). Idempotent (upserts by unique role name / user email). Verified: after seeding, the real-roster upload returns `matchedRows:80 / newEmployeeRows:0 / unmatchedRoleRows:0 / errorRows:0`. Usage: `npx tsx server/scripts/seed-gattopardo.ts`.
- [x] Added 503 retry + fallback model handling to the Gemini vision call in `server/src/parsing/parseVision.ts`. Refactored the single `generateContent` call into a `callGemini(genai, model, ...)` helper, and wrapped it in a retry loop: attempt 1 uses the primary model (`VLM_MODEL`, default `gemini-3.6-flash`); on a transient 503 (`ApiError.status === 503`, detected via `isTransientOverload`) it backs off 1s and retries with the fallback model (`VLM_FALLBACK_MODEL`, default `gemini-3.6-flash-lite`), then backs off 2s and retries the fallback once more. Non-503 errors (auth, invalid model, schema rejection) surface immediately without retrying. If all attempts exhaust on 503s, it throws a `VisionIngestionError` with the last error. Added `VLM_FALLBACK_MODEL` to `.env.example`. Verified: `isTransientOverload` correctly detects a 503 `ApiError` and the fallback model is used on retry (mock test); a real roster upload still returns HTTP 200 with parsed rows. `npm run server:typecheck`, root `typecheck`, and `build` all clean.
- [x] REFACTORED (superseded by the next entry below) — Replaced the Gemini VLM vision path with a deterministic Tesseract OCR + grid parser to fix 85-shift inflation. Since reverted: see next entry.
- [x] REVERTED OCR, back to direct vision ingestion — with the hallucination fix applied at the code layer instead of relying on prompt-following alone. Dropped `tesseract.js` entirely (`npm uninstall tesseract.js`, removed `eng.traineddata`) and deleted `server/src/parsing/ocrExtractor.ts`, `gridDetector.ts`, `gridParser.ts`, `parseGridImage.ts` — Tesseract-based grid geometry detection was the wrong tool for arbitrary hand-made venue layouts (fragile column/row clustering) and added a slow local pre-pass. `server/src/parsing/parseVision.ts` (`parseRosterImage`, Gemini `@google/genai`) is now the single ingestion path for both image uploads and PDFs that have no usable text layer / didn't yield rows from `parseRosterText` — `server/src/routes/schedules.ts` calls it directly with the raw PDF/image buffer, no OCR or rendering step first. Added `server/src/parsing/shiftConstraints.ts` — `enforceNoDoubleShifts()` (moved out of the deleted `gridParser.ts`, kept verbatim) is now called from `mapVlmResponseToResult()` as a hard code-level backstop: max 1 shift per (employee, date), or 2 non-overlapping shifts with a ≥30min break (AM+PM split); anything beyond that (the "one cell split into 3+ rows" pattern that previously inflated a roster to 85+ shifts) is stripped from `rows` and routed to `anomalies` instead of trusting the prompt's own dedup instructions. `parseRosterImage` now logs total elapsed ms per call for latency tracking against the <2s target (network/model latency is the only remaining variable now that there's no local OCR/geometry pre-pass). `templateLabel` renamed to "Direct Vision Ingestion". Verified via `npm run server:typecheck`, root `typecheck`, `lint`, and `build` — all clean. NOT yet re-verified against a real roster image/PDF in this session (no live Gemini call made here) — re-run the "Gattopardo" real-roster smoke test before relying on shift counts in production.
- [x] FIXED three critical pipeline/state-binding bugs so the vision upload parser functions identically to 7shifts. (1) **Confirm & Commit state binding** — `src/components/ShiftUpload.tsx` now accepts an `onCommitted?: (rows: PreviewRow[]) => void` prop and, after a successful `confirmRoster`, flushes the reviewed `matched` preview rows to the parent; `src/App.tsx` keeps a `committed` state (employees + shifts) merged into the displayed roster via `mergedRoster` so the grid rows and weekly totals populate immediately on commit. (2) **Vision parser date/header extraction** — `server/src/parsing/vlmPrompt.ts` DATE HANDLING now explicitly instructs PASS 1 to read the roster's date-range header (e.g. "Roster week of 17 Aug", "Week 17-23 Aug") and anchor every day column to the week's starting date instead of defaulting to unassigned/unresolved; `server/src/parsing/parseVision.ts` `callGemini` now accepts and threads `weekStart` into the user prompt as the reference week so day-month labels resolve to full ISO dates. (3) **Role resolution on upload** — `server/src/parsing/resolveRows.ts` adds a `ROLE_ALIASES` map + exported `canonicalRoleName()` that normalizes free-form parser/VLM role strings ("Floor"→"Floor Staff", "SUPERVISORS"→"Supervisor", "Management / Floor"→"Management", "Head Waiter", etc.) to canonical seeded Role names before the case/whitespace-insensitive DB lookup, so valid staff (Alessandro, Pratik, Roberto) no longer show "Unmatched role ⚠️"; `server/scripts/seed-gattopardo.ts` ROLE_NAMES also gained `Floor Staff`. Verified: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, and `npm test` (8/8) all clean; `canonicalRoleName` unit-checked for the reported role strings.
- [x] SMOKE-TESTED the Confirm & Commit state-binding handler. Added `src/engine/commitBinding.test.ts` (3 tests) that exercises the exact `handleCommitted` + `mergedRoster` logic from `src/App.tsx` and the `onCommitted?.(data.preview.filter((r) => r.status === 'matched'))` flush from `src/components/ShiftUpload.tsx` against a realistic 116-shift preview payload: (1) 116 matched rows → 8 unique employees + 116 shifts flushed into the merged roster, weekly totals compute non-zero; (2) an `unmatched_role` row is excluded from the flush (skipped server-side, would otherwise render a phantom shift); (3) committed shifts merge into an existing text-parsed roster without id collision/duplication. Full chain confirmed wired: button `onClick={onConfirm}` (ShiftUpload.tsx:330) → `onConfirm` calls `confirmRoster` then `onCommitted` (ShiftUpload.tsx:67-84) → `App.tsx:101` passes `handleCommitted` → `setCommitted` → `mergedRoster` (App.tsx:45-58) → grid renders `mergedRoster.employees`/`mergedRoster.shifts` (App.tsx:158-159). Verified: `npm test` (11/11), `npm run typecheck`, and `npm run build` all clean. Note: verified at the logic/type level — a live browser click against a running server+DB was not performed in this session.
- [x] DIAGNOSED the reported `POST /api/schedules/upload/{batchId}/confirm 404` — the Vite proxy is NOT the cause. Live-tested the full upload→confirm flow through the Vite dev proxy (`http://localhost:5173/api/...`): upload of `server/test-fixtures/sample-roster.xlsx` returned HTTP 200 with a real `batchId`, and the immediate confirm returned HTTP 201 `{"message":"Imported 3 shift(s).","createdCount":3,...}`. This proves `vite.config.ts`'s `/api` proxy → `http://localhost:4000` is correctly forwarding backend requests (also confirmed `/api/health` via 5173 returns 200). The real cause of the user's 404 is the **in-memory, process-local upload cache** (`server/src/store/uploadCache.ts`, 15-min TTL): the `tsx watch` server restarted multiple times during the earlier code edits (log shows `EADDRINUSE: address already in use :::4000` when a restarted process couldn't rebind), and each restart wipes the cache — so a batchId created before a restart returns 404 "This preview has expired or was already confirmed" on confirm. Also note the vision parse takes ~90-100s, so a long review window can push past the 15-min TTL. Fix direction (not yet applied): make the upload cache survive restarts (persist to DB/Redis) or surface a clearer "preview expired, re-upload" message in the UI. Test shifts created during verification were deleted from the DB afterward.
- [x] FIXED the upload-cache restart bug with a lightweight JSON-file persistence layer (no Redis/DB migration). `server/src/store/uploadCache.ts` now persists batches to `<server>/.upload-cache.json` (overridable via `UPLOAD_CACHE_FILE` env var, documented in `.env.example`): loads on construction, rewrites on every put/delete/sweep, drops expired entries on load, and tolerates a corrupt/unreadable file (logs and starts empty rather than crashing). Verified: a batch written by one process instance is read back by a fresh instance (simulated restart) — `FOUND` with correct locationId/rows; full upload→confirm through the Vite proxy returns HTTP 201. `.gitignore` now excludes `.upload-cache.json`. Also updated the frontend error handler: `src/components/ShiftUpload.tsx` `onConfirm` catch now detects `ApiError` with `status === 404` and shows a clear warning toast "Upload session expired, please re-upload the roster." (new `sessionExpired` state + `.error-block-toast` accent styling in `src/styles/global.css`) instead of a bare 404. Verified: `npm run server:typecheck`, `npm run typecheck`, `npm run build`, `npm test` (11/11), and `npm run lint` all clean. Test shifts created during verification were deleted from the DB afterward.
- [x] FIXED two VLM layout-parser bugs for PDF/image rota ingestion (Il Gattopardo layout). (1) **AM/PM split-shift separation** — `server/src/parsing/vlmPrompt.ts` now adds a `period` field (`AM`/`PM`/null) to both the OpenAI-style `ROSTER_VLM_JSON_SCHEMA` and the Gemini `ROSTER_VLM_GEMINI_SCHEMA` cell objects, and rewrites the PASS 1 sub-column + "CRITICAL DEDUPLICATION" sections to unambiguously instruct the model to emit ONE entry per sub-column (AM and PM as two independent shifts on the same parent day, each with its own start/end/period) instead of the previous contradictory wording that caused AM/PM to be collapsed into a single block. `server/src/parsing/parseVision.ts` `VlmCell` gains `period`, and the worked_shift emission prefixes `[AM]`/`[PM]` into `managerNotes` so the label survives role/user resolution and shows in the preview. `server/src/parsing/shiftConstraints.ts` `enforceNoDoubleShifts` now converts times to minutes-since-midnight and rolls overnight ends forward by 24h (fixing the naive `HHmm` integer math that misjudged overnight PM splits), so valid AM+PM splits with a real break are accepted while overlaps/too-close/3+ splits are still flagged. (2) **Missing management rows** — the prompt's PASS 1 adds a "ROW BOUNDING — START AT THE TOP STAFF ROW" step instructing the model to begin row detection at the very first data row beneath the date/AM-PM headers and to include management/floor rows (Andrea, Roberto, Tomas, Alessandro) that sit above supervisors, instead of truncating the top of the grid. Added `server/src/parsing/shiftConstraints.test.ts` (5 tests) and `server/src/parsing/parseVision.test.ts` (2 tests). Verified: `npm run server:typecheck`, `npm run typecheck`, `npm run build`, `npm run lint`, frontend `npm test` (11/11), and server tests (7/7) all clean. Note: prompt/schema changes verified at the code+unit level; a live Gemini call against a real Il Gattopardo PDF was not run in this session.
- [x] FIXED two UI/data-mapping issues to match Dubai hospitality standards (7shifts reference). (1) **Manager role mapping** — `server/src/parsing/resolveRows.ts` `ROLE_ALIASES` now maps management titles (`Manager`, `GM`, `General Manager`, `Restaurant Manager`, `Floor Manager`, `Duty Manager`, `Operations Manager`, `Assistant Manager`, `Head of Floor`, `Floor Management`, etc.) → `Management`, so top-tier rows (Andrea, Roberto, Alessandro, Tomas) inherit their designated management titles instead of defaulting to "Floor". `server/src/parsing/vlmPrompt.ts` PASS 1 adds a "ROLE EXTRACTION — USE THE STAFF MEMBER'S ACTUAL TITLE" step instructing the model to read each row's printed title and never default top-tier rows to a generic "Floor" label (only genuine floor service staff get "Floor"/"Floor Staff"). (2) **Categorized roster grid** — `src/App.tsx` refactors the flat grid into grouped role sections (Manager, Supervisor, Head Waiter, Waiter, Runner, Other) via a `ROLE_SECTIONS` ordering + `sectionForRole()` matcher, with collapsible section headers (`collapsed` state) and per-section staff counts; day cells now render AM/PM split shifts as two compact labeled blocks (`.split` + `.period`) via a new `periodOf()` helper that reads the `[AM]`/`[PM]` label from the shift source. `src/styles/global.css` adds `.roster-section`, `.roster-section-head`, `.section-caret`, `.section-label`, `.section-count`, and `.cell.shift .split`/`.period` styles. Verified: `npm run server:typecheck`, `npm run typecheck`, `npm run build`, `npm run lint`, frontend `npm test` (11/11), and server tests (7/7) all clean; `canonicalRoleName` unit-checked (Manager/GM/Floor Manager→Management, Floor→Floor Staff) and `sectionForRole` grouping verified (4 manager, 2 supervisor, 1 head-waiter, 1 waiter, 2 runner). Note: grid layout verified at the code/type level — no browser/screenshot tool available to visually confirm the rendered grouped sections.
- [x] ADDED a `dev:all` script to run the backend API server and frontend Vite dev server simultaneously via `concurrently`. `package.json` now has `"dev:all": "concurrently -k -n api,web -c blue,green \"npm:server:dev\" \"npm:dev\""` (installed `concurrently@^10.0.5` as a devDependency). `-k` kills both on exit, `-n api,web` labels the two processes, `-c blue,green` color-codes their output. This resolves the `ECONNREFUSED` proxy errors that occur when the backend on port 4000 isn't running alongside Vite on 5173. Verified: `package.json` is valid JSON, `concurrently` binary works, and `npm run` lists `dev:all` correctly. Note: at verification time both ports 4000 and 5173 were already occupied by existing node processes (API health on 4000 returned 200), so `dev:all` was not fully launched to avoid port conflicts — the user should stop any existing `server:dev`/`dev` processes before running `npm run dev:all`.
- [x] FIXED the `EADDRINUSE` zombie-process startup failure. `package.json` `server:dev` is now `"kill-port 4000 && tsx watch server/src/index.ts"` (installed `kill-port@^2.0.1` as a devDependency), so the backend automatically kills any existing process on port 4000 before starting the Express server. This also flows through `dev:all` (which references `npm:server:dev`). Verified end-to-end: `kill-port 4000` killed the zombie process on port 4000 (PID 8540), then `tsx watch server/src/index.ts` started and logged "ShiftSync API listening on http://localhost:4000", and `/api/health` returned 200. Test server and log were cleaned up afterward.
- [x] FIXED the Gemini 429 rate-limit crash with a robust fallback mechanism in `server/src/parsing/parseVision.ts`. (1) `isTransientOverload` now treats BOTH 503 (high demand) and 429 (rate limit/quota) as retryable, so the existing backoff/retry loop (primary → fallback model, 1s→2s delays) also retries on 429. (2) Added a cached `FALLBACK_SAMPLE_RESPONSE` (exported `VlmResponse` mirroring the Il Gattopardo layout with AM/PM sub-columns, management/supervisor/head-waiter/waiter/runner staff, and a day-off record) that is routed through the same `mapVlmResponseToResult` mapping as a real model response, so the preview/grid render real data. (3) Added a `VLM_FALLBACK_MODE` env var (`auto` default / `sample` / `off`): `auto` tries Gemini then falls back to the sample on 429/503/no-key/non-transient errors; `sample` skips the network entirely; `off` surfaces the Gemini error. The fallback result's `templateLabel` is set to `Fallback sample (<reason>)` so the UI/manager can see the data is a fallback. Documented `VLM_FALLBACK_MODE` in `.env.example`. Added a `parseVision.test.ts` case verifying the fallback sample maps to a valid result (management rows present, AM/PM splits preserved, day-off captured, ≤2 shifts/employee/day). Verified: `npm run server:typecheck`, `npm run typecheck`, `npm run build`, `npm run lint`, frontend `npm test` (11/11), server tests (8/8) all clean. Live smoke test: started the server with `VLM_FALLBACK_MODE=sample` on port 4100 and uploaded `real-roster.png` — returned HTTP 200 with `templateDetected:"Fallback sample (VLM_FALLBACK_MODE=sample)"`, 10 matched shifts, 0 anomalies, management roles + AM/PM splits + Tomas day-off all present, and no Gemini API call was made. Test server and log cleaned up afterward.
- [x] DISABLED the forced sample fallback and made the live VLM pipeline the default. Root cause: `.env` had `VLM_FALLBACK_MODE=sample`, which forced the hardcoded Il Gattopardo sample on every upload. Changed `.env` to `VLM_FALLBACK_MODE=auto` (the code default). Also discovered a shell env var `VLM_FALLBACK_MODE=sample` was overriding `.env` (dotenv does not override already-set env vars) — unset it. Live-verified: with `auto` and no shell override, an image upload actually called the live Gemini API and parsed the real roster (`[parseVision] Direct vision ingestion complete in 125230ms — 160 shifts, 0 anomalies, 24 leave records`), not the sample.
- [x] MADE the vision parser dynamic/venue-agnostic and added a deterministic local fallback parser. (1) `server/src/parsing/vlmPrompt.ts` PASS 1 now handles THREE daily structures — (a) AM/PM sub-columns, (b) multi-segmented split shifts in one cell ("10am/3pm-7pm/12am", "9-13/18-23"), and (c) single shift per day — and instructs the model to split multi-segmented cells into separate entries. Role extraction is now venue-agnostic (any role/department label, groups in any order, not just Gattopardo's Management/Floor/Supervisor). DATE HANDLING now ignores venue/brand title lines (e.g. "Bar des Pres") as non-date headers. (2) Added `server/src/parsing/deterministicParser.ts` — a venue-agnostic local parser (`parseRotaFile`) handling Excel/CSV/PDF-text, multi-segmented split shifts (including the compact "10am/3pm-7pm/12am" bridge notation → two intervals), 12h/24h time normalization, leave codes, and role categorization (Manager/Supervisor/Head Waiter/Waiter/Runner). Fixed bugs in the reference implementation: the `forlots:` typo, the `normalizeTime` placeholder (now real 12h/24h→HH:mm), and the multi-segment bridge logic. (3) Wired it into `parseVision.ts` as the fallback path via `buildLocalFallback()`: on 429/503/no-key, PDFs are parsed deterministically (extract text → `parseRotaFile`); images (no text layer) still fall back to the cached sample. Added `deterministicParser.test.ts` (8 tests). Verified: `npm run server:typecheck`, `npm run typecheck`, `npm run build`, `npm run lint`, frontend `npm test` (11/11), server tests (16/16) all clean. Live-tested `parseRotaFile` on a Bar des Pres-style FOH roster: `10am/3pm-7pm/12am` correctly split into `10:00-15:00` + `19:00-00:00` (overnight flagged), roles categorized, Off days skipped, dates mapped to the week.
- Build the dark-mode weekly roster grid component (full compliance flags)
- Implement one-click revocable live share links
- Implement the UAE compliance rules engine (8/48 baseline, overtime multipliers, rest days, leave accrual)
- Implement the service-charge pool tracker tied to verified attendance
- [x] MERGED a Lovable-generated UI layer ("shiftsync-dubai-luxury", cloned as a nested repo at `shiftsync-dubai-luxury/`) into the main app as a real dashboard, not a static import. The Lovable repo was a full TanStack Start SSR app (its own router/server/Nitro build, `bun` package manager) — decided NOT to adopt that framework for one page's worth of components; instead treated it purely as a component/token source and ported into the existing Vite SPA. (1) **Tailwind v4 added to the main app** (previously zero Tailwind — `src/styles/global.css` was hand-written CSS): installed `tailwindcss`, `@tailwindcss/vite`, `tw-animate-css`, `clsx`, `tailwind-merge`, `lucide-react`; `vite.config.ts` gained the `tailwindcss()` plugin; new `src/styles/tailwind.css` defines the `@theme inline` tokens (`--color-background`, `--color-accent`, `panel`/`eyebrow`/`gold-rule` utilities, etc.) **pinned to the existing `global.css` hex palette** (`--bg:#0f0f12`, `--accent:#e5a93c`) instead of importing Lovable's separate oklch palette — one source of truth, not two. (2) **6 of the 7 Lovable feature components ported** to `src/components/shiftsync/` (`PersonalRota`, `TeamMatrix`, `HourTracker`, `ApprovalsPanel`, `SafetyValve`, `PredictiveBanner`), rewritten to accept props instead of importing the Lovable repo's `shiftsync-data.ts` mocks — none of them actually depended on shadcn `ui/*` primitives or the ~25 `@radix-ui/*` packages `components.json` implied, so those were never installed. `TemplateIngestion` (7th component, a mock Excel drag-and-drop box) was dropped — it duplicated the already-real `src/components/ShiftUpload.tsx` upload/parse/confirm flow. (3) New `src/engine/rosterView.ts` (`weekdayOf`, `periodOf`, `weekDates`, `shiftsFor`, `totalHours`) factored out of `App.tsx`'s local helpers so both the roster grid and the new dashboard share one date/period-mapping source. New `src/components/Dashboard.tsx` composes the ported components against real `mergedRoster`/`config` data: `PersonalRota` and `TeamMatrix` are driven by actual `Roster.employees`/`Roster.shifts` (with a "Viewing: [employee]" selector since there's no auth/logged-in-user concept yet), `HourTracker` sums real `shiftHours()` per employee against `config.compliance.maxWeeklyHours`. Rendered into `App.tsx` above `ShiftUpload`. (4) `ApprovalsPanel`, `SafetyValve`, `PredictiveBanner` were explicitly scoped as **visual shells only** per the user's own choice (no backend exists yet for swap requests, anonymous feedback, or POS integration) — `ApprovalsPanel` and `PredictiveBanner` were rewritten to show an honest empty/placeholder state instead of the fabricated swap-request list / "+40% covers" stat Lovable hardcoded; `SafetyValve` was already backend-free and kept as-is (local-only textarea, no fake data displayed). Verified: `npm run typecheck`, `npm run build` (29KB CSS + 258KB JS, Tailwind utilities generating correctly), `npm run lint`, and `npm test` (11/11) all clean; `npm run dev` boots and serves HTTP 200 (no runtime crash) — **not** visually confirmed in a browser (no screenshot/browser tool available in this session). The nested `shiftsync-dubai-luxury/` repo is left in place as a reference/source but is not part of the build.
- [ ] Visually verify the new Dashboard (Personal Rota / Team Matrix toggle, Tailwind styling, palette parity with `global.css`) in an actual browser — not yet done, no screenshot tool in this session.
- [x] DELETED the nested `shiftsync-dubai-luxury/` repo now that its components are fully ported and the covering-employee-role nuance is fixed. Risk check before deleting (all clear): (1) `grep`ed the main app's `src/`, `package.json`, `tsconfig.json`, `vite.config.ts` for any reference to the nested path — zero hits, no code coupling; (2) it was untracked in the main repo's git (`?? shiftsync-dubai-luxury/`), so deleting it touches none of the main repo's history; (3) its own nested `.git` had a clean working tree — no local-only uncommitted work to lose; (4) it has a real GitHub remote (`voyagerventuresdxb-max/shiftsync-dubai-luxury`, 3 commits already pushed) — the source is fully preserved remotely regardless of the local copy, and Lovable's own edit-sync workflow operates against that GitHub repo directly, not a local clone, so deleting locally has zero effect on future Lovable iteration; (5) only 806K, no `node_modules` present (never locally installed). Recommended deletion over moving it elsewhere as a "reference" copy, since GitHub already is that reference and a stray local folder would just be a second, driftable copy.
- [x] FIXED the covering-employee-role nuance flagged in the previous entry. Added `requiredRole?: string` to `Shift` (`src/engine/types.ts`) — the role/position the shift actually needs, set at real parse time from data already being extracted: `src/engine/parser.ts`'s `shifts.push()` now sets `requiredRole: parsed.role ?? emp.role` (the per-line role `splitNameRole` already extracts); `App.tsx`'s `handleCommitted` (the upload/confirm path) now sets `requiredRole: row.role || undefined` (the role already threaded through `PreviewRow`). `Dashboard.tsx`'s `rotaCards` now uses `dayShifts[0].requiredRole ?? activeEmployee.role` instead of always `activeEmployee.role`, so a covered shift shows the role it actually needs, not the covering employee's own role. `TeamMatrix`'s `MatrixCell` gained an optional `requiredRole` (joined for double-shift 'D' cells), surfaced as a native `title` tooltip on each cell — the per-employee role label in the row header is unchanged (that's genuinely the employee's own role, not per-shift, so it wasn't wrong). Verified live via Playwright (same request→approve flow as the `ApprovalsPanel` entry above): Jose's covered Friday shift now correctly reads "bartender" (Maria's original requirement), while his own unrelated Monday shift still correctly reads "server" (his own role) — confirms the fallback and the real-data path both work, not just one or the other. `npm run typecheck`, `build`, `lint`, `npm test` (11/11) all clean.
- [x] Build real backends for the 3 stubbed dashboard panels: shift-swap requests (`ApprovalsPanel`), anonymous floor feedback (`SafetyValve`), POS/reservation integration (`PredictiveBanner`) — all currently render honest empty states, no fake data. STATUS: `ApprovalsPanel` done (see entry below); `SafetyValve` restyled to match the reference design's copy but deliberately kept backend-free (anonymous local-only submission is the intended design, not an unfinished stub — see the Home Feed entry below); `PredictiveBanner` still an untouched honest placeholder (no POS integration exists).
- [x] BUILT the Home Feed (9-task plan): assembled `HomeRoute.tsx` into the real screen — `PredictiveBanner` (untouched placeholder) → `Announcements` → `Shoutouts` → `ApprovalsPanel` → `SafetyValve` — replacing the Task-2 placeholder paragraph.
  - **Announcements** (`src/components/shiftsync/Announcements.tsx` + `server/src/routes/announcements.ts`, `Announcement` Prisma model): real one-way manager broadcast, backed by Postgres, not `localStorage`. Post/edit/delete all round-trip through the API; author name resolved server-side from the `User` relation.
  - **Shoutouts** (`src/components/shiftsync/Shoutouts.tsx` + `server/src/routes/shoutouts.ts`, `Shoutout` Prisma model): peer recognition tied to a specific colleague + a specific real shift (`shiftSnapshot` captured at creation time so the note still reads correctly even if the shift is later edited/removed from the roster). No DELETE endpoint exists by design (matches the Staff Directory's own precedent).
  - **Conflict-Free Approvals** (`ApprovalsPanel.tsx`, real `SwapRequest`-backed flow from Task 7/8, `server/src/routes/swapRequests.ts` + `server/src/lib/swapRequestPolicy.ts`): live per-second countdown to the next Wednesday-17:00-Asia/Dubai request-window close, audit-trail line on decided requests, auto-lock display when a shift was already reassigned by a different approved request — the underlying TOCTOU race (two approvals racing the same shift) is closed server-side via an atomic `updateMany` guard inside the decide transaction, not just a UI-level check.
  - **Direct Floor Feedback** (`SafetyValve.tsx`): copy-only restyle to match the reference design exactly — added the "feedback should be constructive, don't target a colleague" disclaimer above the textarea, and the confirmation copy now mentions Monday-briefing review. Still intentionally backend-free/anonymous/ephemeral (no `Feedback` table) — this was scoped as a copy change only, not a new persistence layer.
  - **Root-cause fix carried through this whole plan (Task 1):** the shift-id collision bug (`upload-shift-${rowNumber}` colliding across upload batches, logged above) was fixed at the root — `mergedRoster` shift/employee ids for any row that matched an existing DB record are now the real Postgres ids, not synthesized per-upload strings. This matters for every future phase that keys off a specific shift or employee id (Shoutouts' shift-tagging, swap requests, floor-plan assignments) — those ids are now stable and DB-addressable instead of upload-session-scoped.
  - **Verified:** `npm run server:typecheck`, `npm run typecheck`, `npm run lint` (0 errors, 1 pre-existing unrelated warning), `npm run build`, `npm test` (20/20), and `server/src/lib/swapRequestPolicy.test.ts` + `server/src/parsing/persistShifts.test.ts` directly (8/8) — all clean.
  - **Live end-to-end API verification against the real Supabase DB** (backend dev server, not a mocked test): posted a real announcement, confirmed it via an independent re-`GET` (proving Postgres persistence, not `localStorage`), edited it, re-confirmed the edit persisted, deleted it, re-confirmed the deletion persisted. Posted a real shoutout tied to a real seeded employee, confirmed via re-`GET`. Created a real swap request against a real `Shift` row, confirmed it listed correctly (what the Approvals panel would fetch), approved it via the PATCH endpoint, then independently re-queried Postgres directly (not the API response) and confirmed `Shift.userId` had actually changed from the original owner to the proposed cover — this is the one behavior no amount of code-reading can substitute for, and it worked. All test data (the announcement, the shoutout, the swap request + its audit log row, and the shift reassignment) was reverted/deleted afterward; dev server stopped and port 4000 confirmed closed.
  - **NOT verified — needs a human with a real browser:** the visual rendering of the assembled Home screen (panel order/spacing, the new SafetyValve disclaimer copy actually appearing, the countdown timer visibly ticking second-by-second, Personal Rota's "Request cover" action visually producing a card on Home) — no screenshot/browser tool exists in this environment, consistent with every prior frontend change in this project's history. Everything backend-observable was verified directly against the API/DB instead.
- [x] BUILT the real cover/swap-request flow for `ApprovalsPanel`, replacing the stub. New `SwapRequest`/`SwapRequestStatus` types in `src/engine/types.ts` (`id`, `shiftId`, `requestedBy`, `coveringEmployeeId`, `status`, `createdAt`, `decidedAt`) — tied to real `employeeId`/`shiftId` values already in `mergedRoster`, nothing fabricated. State lives in `App.tsx` (consistent with how `mergedRoster` itself is already client-side, in-memory state, not re-fetched from a live DB — there is no existing GET-shifts endpoint the frontend calls, so this matches the app's actual current architecture rather than introducing a disconnected server-only feature): a `swapRequests: SwapRequest[]` array plus a `reassignments: Record<shiftId, employeeId>` map. `mergedRoster`'s `useMemo` now applies `reassignments` on top of the parsed/committed shifts, so approving a request changes `shift.employeeId` and every downstream view (Personal Rota, Team Matrix, Hour Tracker) recomputes from that single source of truth automatically — verified live via Playwright, not just code-reading: reassigning Maria's Friday bartender shift to Jose immediately zeroed Maria's Hour Tracker bar (8.0h→0.0h), added 8h to Jose's (8.0h→16.0h), flipped Maria's Friday card to "Rest day", added the shift to Jose's Personal Rota, and flipped the Team Matrix cells (Maria Fri: PM→OFF, Jose Fri: OFF→PM) — all from one `onDecideRequest('approved')` call, no manual per-view patching. `handleRequestCover(shiftId, coveringEmployeeId)` creates a pending request; `handleDecideRequest(id, 'approved'|'denied')` resolves it (approve also writes the reassignment; deny just flips status). `PersonalRota.tsx` regained a real (not decorative) "Request cover" action per working shift — click reveals an inline `<select>` of other real employees + "Send request"/"Cancel", replacing the no-op button removed in the previous pass now that there's a real handler to call. `ApprovalsPanel.tsx` rewritten to a real manager view: pending requests show requester/shift/proposed-cover with Approve/Decline buttons; decided requests show an "Approved · shift reassigned" or "Declined" badge; **true empty state** ("No shift-swap requests yet...") when `requests.length === 0`, same honesty standard as the previous stub — nothing renders until a real request exists. Also fixed the redundant "Rest day" / "Rest day" role-heading-plus-status-pill duplication on empty days (flagged during review): the status pill is now only rendered for working shifts (`!isOff`), since the "Rest day" heading alone already says it. Verified: `npm run typecheck`, `npm run build`, `npm run lint`, `npm test` (11/11) all clean; live Playwright run (system Chrome via `playwright-core`) drove the full flow — empty state → open picker → send request → approve → cross-view reassignment confirmed → Team Matrix confirmed — 7 screenshots, matching computed-style/DOM assertions along the way (not just visual read).
- [x] FIXED a critical CSS class-name collision that silently broke every `grid`-based layout in the newly merged dashboard (mode toggle, two-column split, TeamMatrix's staff/day grid, PersonalRota's card layout) — found via real Playwright screenshots (system Chrome, `playwright-core`, no browser install needed) taken specifically to verify the Tailwind merge rendered correctly, not just that the build passed. Root cause: `src/styles/global.css` had a pre-Tailwind `.grid { display: flex; flex-direction: column; gap: 2px; }` rule (written for the old roster-table wrapper in `App.tsx`, predates this session's Tailwind work) that collides by name with Tailwind's own `.grid` utility (`display: grid`). Per the CSS Cascade Layers spec, Tailwind wraps all its utilities in `@layer utilities`, and **unlayered CSS always wins over layered CSS regardless of source order or specificity** — so global.css's unlayered `.grid` rule silently beat Tailwind's layered one on every element carrying Tailwind's `grid` class, collapsing every grid layout into a vertical flex stack (confirmed via `getComputedStyle`: `display: flex` / `flex-direction: column` on all 5 grid-using elements checked, `grid-template-columns` present but inert). Fix: renamed the legacy class to `.roster-table-grid` in both `global.css` and its one usage site (`App.tsx:204`). Re-verified via `getComputedStyle` (all 5 elements now correctly report `display: grid` with the right `grid-template-columns`) and fresh screenshots (mode toggle now 2 side-by-side pills, dashboard correctly 2-column, TeamMatrix's 8-column staff grid renders correctly). Also scanned all other `global.css` class names against Tailwind's utility vocabulary — `.grid` was the only collision. Palette also verified pixel-exact via `getComputedStyle` (not just visual inspection): body bg `rgb(15,15,18)` = `#0F0F12`, panel bg `rgb(26,26,34)` = `#1A1A22`, border `rgb(42,42,54)` = `#2A2A36`, text `rgb(224,224,224)` = `#E0E0E0`, accent `rgb(229,169,60)` = `#E5A93C` — exact matches, no drift to Lovable's separate oklch palette. `npm run typecheck`, `build`, `lint`, `npm test` (11/11) all clean after the fix.
- [x] VISUALLY VERIFIED the Staff Directory panel (built + persisted in an earlier session, explicitly flagged there as "not visually verified — no browser tool available") via live Playwright (system Chrome, `playwright-core`) against the real running backend + Supabase DB. Panel rendering itself is solid: collapsed/expanded toggle, table layout, "Not set" placeholders, add-staff form all match the dark-luxury system with no misalignment; computed-style check confirmed zero new CSS collisions (`.staff-directory-*` classes don't shadow any Tailwind utility, `.grid-head`/the Dashboard's Tailwind `.grid` both still correctly compute `display: grid`). Inline-edit-on-blur genuinely persists to the live DB — set real job titles for Andrea ("Restaurant Manager"), Roberto ("Assistant Restaurant Manager"), Tomas ("Assistant Manager"), Alessandro ("Junior Assistant Manager") via the actual UI, independently confirmed via a follow-up `GET` that all 4 landed correctly server-side. Testing surfaced 2 real, unfixed issues (verification only — no fix applied, per the ask to report back first):
  - **(1) Client-side stale-closure bug, data-loss in the UI only (not the DB).** `handleTitleBlur` and `handleAdd` in `src/components/StaffDirectory.tsx` both call `notify(staff.map(...))` / `notify([...staff, created])`, closing over the `staff` variable from whichever render created that closure, instead of a functional `setStaff(prev => ...)` updater. Caught empirically: after setting all 4 job titles above (screenshot confirmed all 4 correctly displayed), then testing the add-staff-member form with one more entry, 3 of the 4 job titles reverted to "Not set" **in the browser only** — a follow-up direct `GET /api/staff-directory/seed-location` proved the database still had all 4 correct values the whole time. In real usage: a manager editing several people's titles in quick succession, then adding a new staff member, would see their just-typed edits appear to vanish (though a page refresh would show they're actually saved) — confusing and looks like data loss even though it isn't. Fix direction: switch both handlers to the functional `setStaff` form.
  - **(2) Integration gap — the Management-tier scenario described in the ask ("Andrea should show Restaurant Manager instead of blank") is not reachable via the app's current real UI at all**, traced at the code level (not just inferred): the paste-text roster entry point was removed in a prior session (`MEMORY.md` line 58, "per explicit request" — roster now comes only from file upload), so getting Andrea/Roberto/Tomas/Alessandro into `mergedRoster.employees` requires an actual upload. But `resolveRows.ts` resolves each row's role purely from that row's own parsed `roleName` string (`server/src/parsing/resolveRows.ts:111-116`), never from the already-matched employee's existing DB role — and for these 4 specifically, the parser *correctly* emits a blank `roleName` (an intentional, previously-fixed behavior: "the parser declining to guess is the right behavior," per the Staff Directory's own build entry above). A blank `roleName` means no `resolvedRoleId`, which sets `status: 'unmatched_role'` (`resolveRows.ts:137-141`). `src/components/ShiftUpload.tsx:81`'s `onCommitted` callback filters strictly to `r.status === 'matched'` before flushing anything into `App.tsx`'s `committed` state — so **`unmatched_role` rows never enter `mergedRoster.employees` at all**, meaning Andrea et al. wouldn't appear anywhere in the roster grid post-upload, blank-role or otherwise. The Staff Directory's `sectionForEmployee()` cross-reference logic itself is correctly implemented and has nothing wrong with it in isolation — it simply never gets an employee to apply to, for exactly the people it was built to help. Confirmed the roster grid genuinely renders empty (header row only) with zero uploads performed this session, consistent with this trace. Not fixed here (would need a decision: either commit `unmatched_role` rows as unassigned-role employees the way `new_employee` rows conceptually should also be reachable, or have `onCommitted`'s filter make an exception when the Staff Directory already has a Management-pattern title for that name).
  - Verification artifacts: 7 screenshots (collapsed/expanded panel, before/after title edits, add-form filled, after-add regression, empty roster grid baseline) plus `getComputedStyle` checks. Cleaned up after: the one throwaway "QA Test Entry — DELETE ME" record was deleted via a direct Prisma script (no `DELETE` endpoint exists in the Staff Directory API by design) since it's not exposed through the UI; the 4 real job titles (Andrea/Roberto/Tomas/Alessandro) were deliberately **left in place** — they're genuine configuration data matching what the ask described wanting, not test pollution. Both dev servers stopped afterward.
- [x] FIXED the stale-closure bug in `src/components/StaffDirectory.tsx`. `handleTitleBlur` and `handleAdd` now use `setStaff((prev) => { const next = ...; onChanged?.(next); return next; })` instead of closing over the `staff` variable — the previous `notify(list)` helper (which took a plain list, always built from a possibly-stale closure) was removed entirely rather than left around as a trap for the next call site. Also fixed a related gap found while touching this code: the initial `fetchStaffDirectory` load never called `onChanged` at all, so `App.tsx`'s `staffDirectory` state (and therefore the Management-tier cross-reference) stayed empty until the panel was edited at least once in that session, even if job titles were already persisted from a prior session — added the missing `onChanged?.(list)` call there too, plus `onChanged` to the effect's dependency array (fixed the resulting `exhaustive-deps` lint warning; safe to add since `App.tsx` passes the stable `setStaffDirectory` dispatch function). Verified: `npm run typecheck`, `build`, `lint` (0 warnings), `npm test` (11/11) all clean. Live Playwright re-run of the **exact** sequence that reproduced the bug (set all 4 titles, then add a new staff member) — this time all 4 titles stayed correctly displayed through the add, confirmed against a follow-up `GET` too (24 staff incl. the add, all 4 titles intact). Test entry deleted afterward via direct Prisma script (no DELETE endpoint), DB back to 23. Both dev servers stopped.
- [x] FIXED Bug 2 (the Management-tier reachability gap) — proposal was sanity-checked with the user before implementing, per their request, since it touches `resolveRows.ts`'s matching logic. Two-part fix, both narrowly scoped:
  - **Part 1 (`server/src/parsing/resolveRows.ts`, the matching-logic change):** a fallback that only fires when the file's role is genuinely blank (not a typo/unresolved string) **and** the employee already exists with a role on file — uses their existing `roleId` instead of blocking. A non-blank-but-unresolved role (typo, unrecognized label) is completely unaffected and still blocks exactly as before; an unknown employee name is unaffected (`new_employee` territory, untouched). This keeps preview status and actual DB persistence in agreement (`persistShifts.ts` gates purely on `resolvedRoleId`, so a frontend-only patch would have created shifts that display but vanish on refresh — ruled out during the proposal, not attempted). Adds a non-blocking `severity: 'info'` issue on the row explaining the inference ("Role for X was not specified in the file — inferred from their existing staff record") for manager transparency.
  - **Part 2 (`src/App.tsx`, pure frontend, zero matching-logic risk):** the grid-row role text now prefers the Staff Directory's `jobTitle` over the resolved/parsed `emp.role` when the venue has set one: `{staffDirectoryByName.get(nameKey(emp.name))?.jobTitle || emp.role}`.
  - **New `server/src/parsing/resolveRows.test.ts`** (5 tests): the fallback firing correctly; 3 regression guards (non-blank unresolved role still blocks; employee with no existing role still blocks; unknown employee name still blocks); a normally-resolved role is unaffected. All pass, plus the full existing server suite (46/47 — the 1 skip is the gated Ollama live test, unrelated, unchanged) confirms zero regressions elsewhere (Bar des Pres, deterministic grid parser, PDF extraction, shift constraints all still pass).
  - **Verified live end-to-end against the real Gattopardo PDF** (`server/test-fixtures/real-roster.pdf`) via Playwright, not just unit tests: uploaded it fresh — preview response showed `matchedRows: 160, unmatchedRoleRows: 0` (previously Andrea/Roberto/Alessandro would have been 3 of the unmatched count), each with the expected info-issue message. Confirmed the import, then read the actual rendered roster grid: **Andrea → "Restaurant Manager", Roberto → "Assistant Restaurant Manager", Alessandro → "Junior Assistant Manager"**, all three correctly grouped under the "Management" section header (3 members) — exactly the scenario originally described as broken. Tomas has zero shifts in this particular real file/week (confirmed absent from the roster for that reason, not a bug — matches an earlier-session finding that some Gattopardo staff have leave-only weeks). Other staff without a Staff Directory `jobTitle` set correctly fall back to displaying their resolved DB role name (e.g. "SUPERVISORS", "HEAD WAITERS") — Part 2's fallback working as designed.
  - **Cleanup:** this verification pass (plus, it turned out, a large amount of leftover test data from earlier sessions across this project's history — 2,328 total `Shift` rows had accumulated for `seed-location`, a designated test fixture location per its own seed script) was fully wiped via `prisma.shift.deleteMany({ where: { locationId: 'seed-location' } })` — Users/Roles/Location left untouched (legitimate seed data other tests depend on). Both dev servers stopped afterward. `npm run typecheck`, `server:typecheck`, `build`, `lint`, `npm test` (11/11), and the full server suite (46/47) all clean.
- [x] FIXED the remaining Bug 2 gap: `unmatched_role` upload rows (ones that STILL fail to resolve even after the fallback above — a genuine typo, an unrecognized role, or a totally unknown employee) were silently excluded from the roster grid after confirm, with no trace a manager could act on. Two directions were sketched, the user asked for a recommendation before implementing: (A) commit these rows into the grid too, but visually flagged and clearly marked as not-persisted; (B) make them persistable server-side (nullable `Shift.roleId` or a placeholder Role) — a real schema change, and arguably a worse outcome (a role-less shift silently saved is worse than not saved). Recommended and implemented **Direction A** — it satisfies both hard constraints: zero changes to `resolveRows.ts`'s matching logic (this only changes what happens to a row *after* it already failed to match), and it solves the actual problem (visibility, not persistence).
  - `src/engine/types.ts`: added `Employee.needsRoleReview?: boolean` — purely additive, optional, doesn't touch the parser contract.
  - `src/components/ShiftUpload.tsx`: `onConfirm`'s `onCommitted` filter broadened from `status === 'matched'` only to `status === 'matched' || status === 'unmatched_role'` — `unmatched_role` rows are still NOT persisted server-side (`persistShifts.ts` is untouched, still gates purely on `resolvedRoleId`), they're just no longer silently dropped from what's handed to the parent.
  - `src/App.tsx`: `handleCommitted` tags `needsRoleReview: row.status === 'unmatched_role'` on the built `Employee`. The `sections` grouping now pulls flagged employees out of the normal `ROLE_SECTIONS`/`sectionForEmployee` grouping entirely (an unresolved-role employee sorted into "Other" would look identical to a normal Other-bucket employee — no distinct warning) into their own dedicated section, shown **first** (`'Needs Review — Role Unresolved'`), styled with the existing warning palette (`.roster-section-head-warn`, reusing the `.badge`/`.badge-warn` classes already used by the upload preview table, for visual consistency rather than inventing new colors). Each flagged row shows the raw unresolved role text, plus an explicit `⚠ Not saved — role unresolved` badge with a title tooltip explaining what to do (fix the role in the source file, or add the person to the Staff Directory, and re-upload) — deliberately unambiguous that this is a preview-only, unsaved state, since these shifts genuinely aren't in Postgres and a page refresh will lose them (that's correct/expected, not a bug, given the DB constraint).
  - `src/engine/commitBinding.test.ts`: the existing "unmatched-role rows are NOT flushed" test was **testing stale, hand-duplicated logic** (its own local `toCommitted`/filter copy, never actually importing from the real source files) — it was still passing after this change for the wrong reason (asserting behavior that's no longer true) rather than catching the drift. Rewrote it as "unmatched-role rows ARE flushed, flagged needsRoleReview, not silently dropped," updated the local `toCommitted` mirror to set the flag, and updated the simulated confirm filter to match the real one. Also verified no other employee gets incorrectly flagged.
  - **Live-verified against the real Gattopardo PDF, not just unit tests**, in two parts (isolated per-session, deliberately — see the id-collision finding below for why): (1) **regression check** — fresh upload of the real PDF still resolves Andrea/Roberto/Alessandro exactly as before ("Restaurant Manager"/"Assistant Restaurant Manager"/"Junior Assistant Manager", all under Management, 0 unmatched rows) — Bug 2's fix is untouched by this change. (2) **new-behavior check** — a small synthetic CSV row with a deliberately unresolvable role (`"Zzznotarole"`, non-blank, for a fresh employee name) now renders in a real running browser as its own "Needs Review — Role Unresolved" section (count 1, warning-colored header confirmed via `classList.contains('roster-section-head-warn')` — not just a screenshot read), the row showing the raw bad role text, the "Not saved" badge, and — importantly — the *correct shift time and hours* (`09:00-17:00`, `8.0h`), confirming the whole row (not just a placeholder) renders correctly.
  - **Found and separately documented, not fixed (out of scope for this ask):** while combining both uploads in one browser session to test regression + new-behavior together, discovered that `Shift` ids are generated as `upload-shift-${row.rowNumber}` — `rowNumber` is scoped per upload batch, not globally unique, so two separate uploads in the same session whose rows happen to share a `rowNumber` (very likely — most files start numbering at 1) silently collide in `mergedRoster`'s dedup-by-id merge, and the second shift is dropped (the *employee* still appears correctly, only the shift disappears — surfaced here as a flagged employee showing `0.0h` with no time in the grid, even though their row was genuinely committed). Confirmed this is real, pre-existing, and unrelated to today's change by reproducing the flagged-row behavior in complete isolation (single upload, fresh session) — worked perfectly, `8.0h`/correct time, no collision possible. Logged below as a new goal.
  - Full check suite re-verified after all changes: `npm run typecheck`, `server:typecheck`, `build`, `lint`, `npm test` (11/11, including the rewritten test) all clean.
  - **Cleanup:** 320 test shifts (2× the real 160-shift Gattopardo batch, from the two verification runs) wiped from `seed-location` the same way as the previous entry. Both dev servers stopped.
- [x] (FIXED — see entry below) Shift-id collision: `App.tsx`'s `handleCommitted` generated `Shift.id` as `upload-shift-${row.rowNumber}`, only unique *within* one upload batch. Two uploads sharing a `rowNumber` silently dropped the second shift in `mergedRoster`'s merge.
- [ ] Minor cosmetic nit on the new "Needs Review" badge (`⚠ Not saved — role unresolved`): in the narrow name column it line-wraps to 3 lines, a bit cramped. Functional and readable, just not fully polished — worth a shorter label or a tooltip-only treatment if this gets more visual attention later.
- [x] FIXED the shift-id collision bug logged above. Confirmed first, before implementing, that a batch-scoped composite id (`${batchId}-${rowNumber}`) is the better fix over a random UUID: nothing in the codebase parses the existing `upload-shift-${rowNumber}` format structurally (checked — no `.split()`/regex/prefix-matching anywhere, it's only ever constructed once and compared for equality), so neither option is more "disruptive" in that sense. But `mergedRoster`'s merge relies on id equality as a deliberate dedup mechanism (a batch confirmed twice — double-click, retry, re-render timing — should collapse to one copy, not duplicate). A composite id preserves that (same batch + same row → same id, deterministic); a random UUID would silently break it, trading today's silent-data-loss bug for a silent-data-duplication bug on re-confirm. `batchId` is already a real, server-generated id unique per upload, so it's the correct, already-available namespace — no new dependency, no new server-side change needed.
  - `src/components/ShiftUpload.tsx`: `onCommitted` now passes `batchId` alongside the rows (`onCommitted?.(rows, data.batchId)`), typed as `(rows: PreviewRow[], batchId: string) => void`.
  - `src/App.tsx`: `handleCommitted(rows, batchId)` now builds `Shift.id` as `upload-shift-${batchId}-${row.rowNumber}` instead of just `upload-shift-${row.rowNumber}`. `Employee.id` (`upload-emp-${employeeName}`) deliberately left untouched — it's intentionally name-scoped so the same real person across separate uploads correctly dedupes into one employee record, which is correct existing behavior, not part of this bug.
  - `src/engine/commitBinding.test.ts`: updated the local `toCommitted` mirror to accept and use `batchId` the same way (kept in sync with the real source, learned from last time that a stale hand-copied mirror silently masks regressions). Added a new dedicated test — two separate batches (`'batch-aaa'`, `'batch-bbb'`) each starting their own `rowNumber` at 1 (the exact collision precondition) — asserting the resulting shift ids differ and both shifts survive a sequential merge (`afterA` → `afterB`) with both employees and both shifts present. All 12 tests (was 11) pass.
  - **Live-verified, not just unit tests**, in two parts: (1) **collision check** — uploaded and confirmed the real Gattopardo PDF (160 rows, rowNumbers starting at 1), then a second real, different file (`sample-roster.xlsx`, an Excel/CSV-path fixture — swapped in for the originally-planned Bar des Pres PDF, which hit an unrelated, pre-existing local-Ollama vision failure on this machine, "Failed to load image or audio file," logged separately below, not fixed here) in the **same browser session**. Confirmed via the actual rendered grid: all 18 employees present (16 Gattopardo + 2 new from the second file, `Fatima Al Suwaidi` and `Ahmed Khan`), zero rows with a suspicious `0.0h`/missing-time (the exact symptom the bug produced before). Independently confirmed at the **database** level too — `prisma.shift.count` matched the expected total exactly across two full test cycles (160+160+3=323 across repeated uploads in one investigation pass; then 160 alone in the isolated regression run below) — no shift silently dropped at either layer. (2) **regression check** — single upload of the real Gattopardo PDF alone in a fresh session: 16 employees, Andrea correctly `57.0h` (matches every prior verification this session), zero `0.0h` rows, and the DB persisted exactly 160 shifts — single-upload behavior is unaffected by the batchId change.
  - **Found, logged, not fixed (separate, unrelated issue):** the Bar des Pres PDF (`server/test-fixtures/bar-des-pres-roster.pdf`) currently fails at upload with `[parseVisionOllama] ... VisionIngestionError: Ollama request failed (400): Failed to load image or audio file` — a local Ollama vision-path issue on this machine, unrelated to today's fix (this file is image-only/no text layer, so it routes to the Ollama vision path per earlier sessions' design). Worth a follow-up if that fixture is needed again.
  - `npm run typecheck`, `build`, `lint`, `npm test` (12/12) all clean. Test data (323 + 160 = 483 shifts across the verification runs) wiped from `seed-location` afterward, same practice as before. Both dev servers stopped.
- [x] ADDED `AnomalyRecord.rowNumber: number | null` (`server/src/parsing/types.ts`, mirrored in `src/api/schedules.ts`) so the frontend Confirm screen can join anomalies onto `PreviewRow`s by rowNumber at render time instead of needing a merged schema. Before this, `mapVlmResponseToResult()` (`parseVision.ts`) discarded row-level linkage entirely when it split a cell into an anomaly vs. a row — confirmed by reading the code, not assumed. Populated at every anomaly call site across `parseVision.ts`, `shiftConstraints.ts` (`enforceNoDoubleShifts`), `deterministicGridParser.ts`, and `deterministicParser.ts`: `null` when the anomaly has no surviving row (the common case — a `continue`/`return`'d cell, or a row stripped by the double-shift guard-rail), the row's actual number when a cell produces **both** a row and a `needsReview` anomaly (the one place a real link exists — `parseVision.ts`'s `cell.needsReview` branch). Also surfaced a real, tangential type-drift while investigating: `src/api/schedules.ts`'s `PreviewRow.issues[].severity` is still typed `'error' | 'warning'` only, while `server/src/parsing/types.ts`'s `IssueSeverity` already includes `'info'` (from the resolveRows.ts Part 1 fix) — noted here as a follow-up, not fixed (out of scope for this task). `npx tsc --noEmit` clean on both `server/` and root, full parsing suite (46/47, 1 gated live-Ollama skip, unchanged) green.
- [x] EVALUATED IBM Docling (MIT-licensed, self-hosted PDF/table extraction — github.com/DS4SD/docling) as a candidate tier-2 parser, per live web research into production roster-parsing approaches. Installed into an isolated venv (`server/docling-sidecar/.venv`, Python 3.14 — full dependency chain incl. torch 2.13 has cp314 wheels, no compat issues) rather than the global environment. **Install/runtime facts:** 1.30 GB venv, 566 MB one-time model download on first conversion (505 MB layout+TableFormer weights from Hugging Face, 61 MB RapidOCR weights from ModelScope, both cached locally afterward — confirmed zero network calls on a warm-cache re-run). **Accuracy, tested against both permanent fixtures with `TableFormerMode.ACCURATE` forced on:** on Gattopardo (text-layer PDF, borderless positionally-laid-out grid — the deterministic pdfjs parser's own target case), Docling's layout model classified all 433 text spans as standalone `text` at 100% confidence and never detected a `Table` region at all — the markdown/grid output is a scrambled bag of names/numbers with zero row/column structure, strictly worse than the existing deterministic parser (which already handles this exact file correctly, 160 rows). On Bar des Pres (scanned, no text layer — currently only reachable via the 10-minute Ollama fallback), Docling detected one table region (0.86 confidence) and produced a genuinely well-structured 22x9 grid via OCR+TableFormer, correctly capturing `UL`, `OFF`, hyphen-chains (`10:30-4:00-8:00-12`), multi-segment slash shifts (`10am/3pm-7pm/12am`), and free-text times (`4pm to 2am`) — in ~47s warm-cache versus Ollama's 10+ minutes. **Conclusion, reported to and confirmed by the user before implementing:** Docling adds negative value on text-layer PDFs (one clear negative data point, no positive evidence anywhere) but strong value specifically on the no-text-layer/scanned path. Scoped accordingly, NOT as a universal deterministic-fails→Docling→Ollama tier.
- [x] BUILT the Docling sidecar and wired it into the `!hasPdfTextLayer` branch only, ahead of Ollama — the text-layer-PDF/Excel deterministic-fails path is untouched and still goes straight to Ollama, per the evaluation above. User chose the long-running local HTTP sidecar over a per-parse spawned subprocess (keeps model weights resident in memory across requests, mirrors the existing Ollama integration's shape) over abandoning Docling, when asked how to bridge a Python-only library into the Node/Express backend (explicit prior decision this session to stay Node-only was flagged as a real conflict before proceeding). `server/docling-sidecar/service.py` — a small FastAPI app (`/health`, `POST /convert`), binds to 127.0.0.1 only. Does structure extraction ONLY: returns `table.data.grid`'s cell text merge-expanded across each cell's `row_span`/`col_span` into a plain 2D JSON array — the exact same convention `buildMergeExpandedGrid()` already uses for Excel's `!merges` — so zero shift/role/leave-code semantics are duplicated on the Python side; all of that logic stays in the existing, already-tested TypeScript grid interpreter. `server/src/parsing/doclingClient.ts` — `isDoclingAvailable()` (cheap 3s-timeout reachability check), `extractGridViaDocling()` (POSTs the PDF buffer, 90s timeout — measured warm-cache latency plus headroom), `parseScannedPdfViaDocling()` (the full attempt: reachability → convert → `parseExcelGrid(grid, weekStart)`, returns `null` on unavailable/no-table-found so the caller falls through to Ollama exactly like an unreachable Ollama host does today — never a hard failure). `server/src/routes/schedules.ts`'s PDF branch restructured into 3 explicit cases: deterministic grid result (unchanged) → `!hasTextLayer` (NEW: Docling, then Ollama on `null`/`DoclingUnavailableError`) → `hasTextLayer` but deterministic failed (UNCHANGED: text-list parser, then Ollama). `npm run docling:sidecar` script added (`server/docling-sidecar/.venv` python + uvicorn on port 8901 with `kill-port` first, matching `server:dev`'s pattern) — first attempt used forward-slash paths and hit a real Windows `cmd.exe` quirk (forward slashes work fine as *arguments* to an already-resolved binary like `tsx`, but not when cmd itself has to resolve the executable path), fixed with a quoted backslash path. `.venv`/`__pycache__`/`*.pyc` added to `.gitignore`. **Live-verified, not just unit tests:** new `server/src/parsing/doclingClient.test.ts` (gated — skips cleanly if the sidecar isn't running, matching the existing gated-live-Ollama-test pattern) ran the real Bar des Pres PDF through the real running sidecar twice (once started manually, once via the new `npm run docling:sidecar` script specifically to confirm the script itself works, not just my manual invocation) — both passed, ~39-59s each. Follow-up diagnostic run showed 62 resolved rows / 17 anomalies / 65 leave records for 12 of the fixture's 20 staff; the other 8 (e.g. Robert Orgovan, Aya Boutaieb) simply have zero clean-resolvable shifts this particular week in the source data itself (all-`UL`/all-shorthand-flagged weeks) — confirmed this is real roster content, not a Docling extraction gap, by cross-checking against the hand-transcribed `barDesPresReference.test.ts` fixture. A handful of `"70"`/`"10"` cells were correctly flagged as unresolvable rather than guessed — an undocumented shorthand code not in the venue's given definitions (UL/CL/IN), consistent with the "never silently guess" principle. `npx tsc --noEmit` clean, full parsing suite 47/47 pass + 1 gated skip (0 regressions). Cleaned up all throwaway verification/inspection scripts from `server/docling-sidecar/` after use, kept only `service.py`/`requirements.txt`/`.venv`.
- [x] (INVESTIGATED then FIXED — see the two entries below) INVESTIGATE the Bar des Pres PDF upload failure found above: `[parseVisionOllama] ... Ollama request failed (400): Failed to load image or audio file` when uploading `server/test-fixtures/bar-des-pres-roster.pdf` on this machine. This file is image-only (no text layer), routes to the local Ollama vision path by design — the failure looks like a local Ollama/model image-handling issue, not a code-logic bug, but not yet diagnosed. Worth a look next time that fixture is needed for real testing.
- [x] LIVE-VERIFIED the Docling sidecar end-to-end through the real browser UI (not just the test suite), per explicit request. Started `npm run dev:all` + `npm run docling:sidecar`, installed `playwright-core` (`--no-save`, system Chrome, no browser download), uploaded the real Bar des Pres PDF through the actual `ShiftUpload.tsx` dropzone. Hit a real environment gotcha: Vite binds to IPv6 loopback only (`::1`) on this machine, so `http://localhost:5173`/`127.0.0.1` both fail from a script — `http://[::1]:5173` works (the app's own API calls are unaffected, since they go through Vite's own Node-side proxy to `:4000`, not a browser-resolved `localhost`). Added a one-line `console.log` in `schedules.ts` at the Docling-success branch (permanent, mirrors the existing `[parseVisionOllama]`-style tier logging) to get an unambiguous trace. Confirmed via screenshot + server log together: `[schedules] PDF resolved via Docling sidecar: 62 rows, 17 anomalies, 65 leave records` — exact match to the direct-sidecar test — real staff/dates/times/hyphen-chains rendered correctly in the live preview table, zero special-casing needed on the frontend. Noted one pre-existing, unrelated `resolveRows.ts` nuance while reading the screenshot (Eugeniu Mihalas/"Supervisor" showed `new_employee` instead of `unmatched_role` since "Supervisor" resolves via `ROLE_ALIASES` while everyone else's raw title doesn't) — not a bug, just `resolveRows.ts`'s existing status-precedence logic working as designed.
- [x] RESEARCHED Dubai/GCC hospitality roster formats live (not training data) to find structural gaps beyond Gattopardo/Bar des Pres. Findings: POS export formats (Foodics, Oracle Simphony/MICROS) are proprietary/not publicly documented — same situation as the two real fixtures already held, only a real venue export shows the true shape. 7shifts sells a named "Multi-Location Restaurant Schedule Template," confirming multi-outlet-single-file is a real, common pattern. setupmyhotel.com documents a single-letter legend-code shift convention (M/E/N/G, hours defined separately, not in the cell) as a standard hotel/F&B duty-roster style. Sourced real Dubai fine-dining brigade titles (Outlet Manager, Captain, Chef de Rang, Demi-Chef de Rang, Commis de Salle, Chef de Partie, Commis Chef) — confirmed via direct grep that NONE exist in `ROLE_ALIASES`. Ramadan is explicitly **not** a distinct template pattern — UAE law mandates a 2hr/day reduction implemented via the *same* formats (earlier closes/split shifts), so the correct test is split-shift density in existing shapes, not a new layout.
  - Built 7 synthetic-but-realistic stress fixtures grounded in the above (`server/scripts/make-stress-fixtures.mjs`, `make-stress-pdf.mjs` → `server/test-fixtures/stress/`): multi-outlet single-file, legend-code shifts (xlsx + text-layer PDF variant), bilingual Arabic/English, days-as-rows, Dubai fine-dining role hierarchy, Ramadan split-shift density. Ran all 7 through the real `/api/schedules/upload` endpoint on the live dev server (not internal function calls).
  - **Found a systemic bug**: 4 of 6 Excel fixtures got employeeName/role silently swapped — resolved as normal `matched`/`unmatched_role` rows, not flagged at all. Root cause traced precisely: `hasTitleColumn = firstDayColIndex >= 2` in `deterministicGridParser.ts` decides column meaning by position only, correct for Bar des Pres's own "Role, Name, days..." ordering but silently backward for the equally common "Name, Role, days..." reverse. Also found: multi-outlet files only read their first outlet block (title/header rows of outlets 2+ misread as staff-row garbage); legend-code cells correctly flagged as unresolved (never-guess held) but the legend table itself gets misread as a second shift grid; bilingual Arabic/English and days-as-rows both defeat the deterministic tier entirely and fall through to a live Gemini call — a 4th tier (Excel/CSV's own pre-existing `parseRosterGrid` fallback) not previously accounted for when "the three-tier pipeline" was described; uneven PDF column spacing can fuse adjacent columns. Ramadan-density split shifts themselves: clean, no bug. Published as an Artifact gap report (ranked findings, per-fixture breakdown, coverage matrix) — report only, no fixes applied per the ask. Explicitly flagged as untested this round: Docling/Ollama got no new coverage since none of the 7 fixtures were genuinely scanned/image-only (only Bar des Pres remains verified there).
- [x] FIXED the column-swap bug found above. Replaced the position-only `hasTitleColumn`/`nameColIndex`/`titleColIndex` assignment in `deterministicGridParser.ts` with a content-based check for the specific 2-leading-column case: sample every row where BOTH candidate columns are populated (a genuine per-row title+name pair — the most reliable signal, and excludes section-header-only rows like "FLOOR" from corrupting the sample, since a header's own column placement doesn't reliably indicate which column means what) and score each column against the known role vocabulary via a new `isRecognizedRoleAlias()` export (`resolveRows.ts` — a query helper only, the alias table itself untouched). A zero-vs-nonzero hit split on either side confidently picks that side; both columns having hits falls back to comparing rates with a 0.25 margin; both zero (no paired rows at all, or a file whose real vocabulary genuinely isn't in `ROLE_ALIASES` yet) is genuinely ambiguous — no signal to guess from either way.
  - **Ambiguous rows are surfaced via the existing `AnomalyRecord` mechanism** (`employeeName: null`, both raw column values in `rawText`, `rowNumber: null` since no row is created) rather than resolved as `Matched` — this IS the codebase's existing "needs manual review" signal (the upload preview's "⚠ Needs manager review" block), used instead of adding a new field since `ParsedShiftRow` has no `needsReview` boolean (only the VLM-path's transient `VlmCell` shape does). Deliberately does **not** early-return with a distinct bail-out `templateLabel` the way "no day-header row" does — it stays `'Deterministic Grid Parser'` (the header/day-columns really were found correctly; only the leading-column semantics are uncertain) so the ambiguity anomaly reaches the actual user in the upload preview instead of silently triggering fall-through to Gemini/Ollama, which would just guess anyway and defeat the entire point.
  - **Two real bugs caught and fixed during self-verification, not just claimed correct:** (1) an initial margin-based version regressed 4 Bar des Pres tests — root cause: Bar des Pres's own real per-row titles ("RM", "JAM", "Head waiter 1"...) mostly aren't in `ROLE_ALIASES` either, so its title column's hit *rate* was low (~1/20) and didn't clear a flat 0.25 margin; fixed by making a zero-vs-nonzero split decisive on its own rather than rate-margin-based, since a real name column has an ~0% false-positive rate regardless of the title column's absolute hit rate. (2) a first attempt at the paired-rows-only sampling caused one small synthetic unit-test grid (zero genuine paired rows, header + single-column-only rows) to always read as ambiguous even though it's a completely standard, unambiguous shape — fixed with a two-tier fallback: prefer paired-row signal when any exist (the common case — a real roster's staff rows almost always carry both title and name per row), fall back to a full-column scan (noisier, header rows included) only when zero paired rows exist anywhere on the sheet.
  - Verified against the same 6 stress fixtures via the real live API (not just direct function calls): fixtures 1/2/6 now correctly resolve `employeeName`/`role` in the right order (spot-checked via anomaly `employeeName` attribution for fixture 2, since its main-grid cells are legend-codes that don't produce clean rows — confirmed "Ahmed Ali" not "Waiter"). Fixture 5 (Dubai fine-dining titles) is genuinely ambiguous by design — none of Outlet Manager/Chef de Rang/Demi-Chef de Rang/Commis de Salle/Chef de Partie/Commis Chef are in `ROLE_ALIASES` (confirmed by grep) — correctly flagged (0 rows, 9 anomalies) rather than silently guessed, which is the intended outcome for a genuine vocabulary gap, not a bug to chase within this task's scope. Fixtures 3/4 (bilingual, days-as-rows) unaffected, as expected — neither reaches this code path at all. Full parsing suite: 47/47 pass + 1 gated live-Ollama skip, **0 regressions**, confirmed via 3 full re-runs across the debugging process (including the real Bar des Pres reference tests and the live Docling-sidecar integration test, which recovered automatically once the regression was fixed — same root cause, not a separate bug). `npx tsc --noEmit` clean throughout. Untouched, exactly as scoped: `ROLE_ALIASES` contents, multi-outlet handling, legend-table misread, the bilingual/Gemini fallback path.
- [x] ROOT-CAUSED and FIXED the Bar des Pres Ollama upload failure logged above (`Ollama request failed (400): Failed to load image or audio file`). Confirmed step-by-step before touching code, per explicit instruction: (1) `qwen2.5vl:7b` IS pulled and Ollama IS running (`GET /api/tags`) — not an environment/model issue. (2) `parseVisionOllama.ts` base64-encodes whatever buffer it's handed with no PDF-to-image conversion step anywhere; `schedules.ts` passes `req.file.buffer` — the raw uploaded PDF, confirmed via its literal `%PDF-1.3` header bytes — straight into it for every Ollama-fallback branch. (3) Reproduced the exact error **directly against Ollama's API, outside the app**, by sending the same base64(raw PDF) payload via curl. (4) Confirmed the fix direction by rasterizing the same PDF's first page to a real PNG (via `pypdfium2`) and sending that instead — got past image-loading entirely, failed on an unrelated already-solved issue (context size, only because the manual test omitted `num_ctx`). **Verdict: a bug in the app's own request-building code, not an Ollama/environment issue** — the PDF-to-Ollama fallback path was built assuming `req.file.buffer` is already a raster image, true for the direct-image-upload branch but false for the PDF-with-no-text-layer branch.
  - Fix: new `server/src/parsing/pdfRasterize.ts` (`rasterizePdfPageToPng`), called at both PDF→Ollama fallback sites in `schedules.ts` (not the direct-image-upload branch, which already has a real image). **Implementation pivot, reported honestly rather than silently changed:** the original plan (`pdfjs-dist` + `@napi-rs/canvas`, keeping this Node-only per the project's established preference) segfaulted reproducibly in `page.render()` — isolated via 3 separate tests (full-size canvas, a much smaller canvas ruling out a size/memory cause, and a version with try/catch that never caught anything since the crash is below the JS layer) — confirmed via live web search as a known, documented category of pdfjs-dist/canvas-backend incompatibility, not specific to this file. Per the user's explicit "whichever is the better fit" authorization, pivoted to the Docling sidecar's already-installed `pypdfium2`, invoked as a short-lived **subprocess** per call (`server/docling-sidecar/rasterize_pdf.py`) rather than routed through the sidecar's HTTP server — rasterization has no model weights to keep warm unlike Docling's table extraction, and routing through the sidecar would make the "last resort" Ollama fallback newly depend on the sidecar's uptime, an unwanted coupling between tiers meant to be independent. `@napi-rs/canvas` was installed then fully removed after the segfault was confirmed.
  - **Verified working two ways, not just claimed:** (1) sent the app's actual `rasterizePdfPageToPng()` output straight to Ollama with a cheap one-word prompt — correctly identified as `"Table/grid of text."`, no image-loading error. (2) Stopped the Docling sidecar and uploaded the real Bar des Pres PDF through the live API, forcing the genuine `!hasTextLayer → Docling unavailable → rasterize → Ollama` path end-to-end: result was a 10-minute timeout, not the old 400 — a timeout only happens after Ollama accepts and holds the connection open while working; an outright rejection returns in milliseconds. The one-word test's own timing (22s model load + 36s prompt eval, just for one word) explains why the full structured-extraction request takes far longer — this hardware's pre-existing, already-documented slow-inference characteristic (explicitly called out in `parseVisionOllama.ts`'s own comments as an accepted tradeoff), not a new problem. Full parsing suite: 47/47 pass + 1 gated skip, 0 regressions, `tsc --noEmit` clean. Docling sidecar restarted and confirmed healthy afterward.
- [x] INVESTIGATED a reported "Failed to fetch" bug on the roster upload flow, said to be blocking a "Lovable-to-localhost UI merge." **Could not find this documented anywhere accessible** — zero matches for "Failed to fetch" repo-wide, zero matching commits (`git log --all`, only 5 commits total exist, none mention it), and the only 3 "Lovable" mentions in this file (entries 108/110/111) describe that merge as already complete and Playwright-verified, not pending — `ShiftUpload.tsx` (the real upload flow) was never even part of the Lovable port; its mock upload component (`TemplateIngestion`) was explicitly dropped as redundant with it. Flagged this discrepancy plainly rather than assuming the premise. Still attempted live reproduction as asked: drove the real browser (`playwright-core`, system Chrome) through the actual `ShiftUpload.tsx` dropzone for 3 structurally different real files — Gattopardo PDF (160/160 matched, deterministic tier), `sample-roster.xlsx` (3 rows, standard template), Bar des Pres PDF (62 rows via Docling) — all three: HTTP 200, zero failed network requests, zero relevant console errors (one unrelated 404, almost certainly `favicon.ico`). Confirmed structurally, not just by luck, why CORS/wrong-host theories don't apply to this app's real upload flow regardless of timing: `vite.config.ts`'s `server.proxy` forwards `/api/*` to `:4000` server-side (Node-to-Node), so the browser only ever fetches from its own origin in dev — never a cross-origin request at all — and `server/src/app.ts`'s `cors()` has no origin restriction regardless. Reported to the user as safe to proceed with the Lovable merge, with the documentation gap surfaced explicitly rather than silently resolved either way. **Status: investigated and cleared — non-reproducible against real files through the live upload flow. The Lovable-to-localhost merge is unblocked on this item; the merge itself has not been started (separate task).**
  - **One known minor side effect, not a correctness bug:** on a genuinely ambiguous sheet, section-header-only rows (e.g. "SENIOR MANAGEMENT") also get an anomaly rather than being silently recognized as headers, since the pre-existing `hasSeenAnyStaffRow` ALL-CAPS guard (the original "COVERS bug" protection) never flips true when every row on that sheet is flagged instead of resolved. No data is fabricated either way — the sheet already produces 0 rows and multiple explanatory anomalies regardless — left as-is rather than expanded beyond this task's explicit scope.
- [x] BUILT Floor Plan — Sections & Duties (Phase 1 + Phase 2, per `floor-plan-sections-kickoff-brief.md`), ran through the full build order in one pass with only Phase 3 (notifications) deliberately gated.
  - **Schema** (`prisma/schema.prisma`, migration `add_floor_plan`): `FloorPlanImage` (server-stored — the brief explicitly corrects the "stays on device" mockup copy; served via `express.static('/uploads', ...)` under `server/uploads/floor-plans/`, .gitignored) → `FloorSection` (freeform polygon, `polygon: Json` stored as **fractional (0-1) coords** of image width/height, not absolute pixels — deliberate so the DOM-overlay assignment screen never needs a resize-tracking recompute step, just CSS percentages) → `SectionAssignment` (`@@unique([sectionId, staffId, shiftDate])`, `status: DRAFT|PUBLISHED`). The `status`/`publishedAt` fields go beyond the brief's literal `section_assignments` column list — added because Phase 3's "Publish" action (explicitly asked to be built now, minus the push step) needs a persisted draft/published distinction to act on. Flagged as an additive, easily-reversible schema call rather than silently added.
  - **Backend** (`server/src/routes/floorPlan.ts`, mounted at `/api/floor-plan`): upload (rasterizes a PDF via the existing `rasterizePdfPageToPng` — same pypdfium2 step the Ollama fallback uses, per the brief's explicit reuse instruction — with a clear 422 if the sidecar's unavailable, not a crash), section CRUD, `GET /:locationId/assignments?date=` (sections pre-joined with that date's assignments so the frontend does zero client-side joining), assignment upsert (shared write path for both interaction modes, keyed on the same unique triple so re-assigning the same person/section/day updates instead of erroring), unassign, and `/:locationId/publish` (bulk DRAFT→PUBLISHED for a date, assignment writes only — no notification call anywhere in this path).
  - **Frontend** (`src/components/FloorPlan/`): `SectionEditor.tsx` is the only Konva consumer (`react-konva`+`konva`+`use-image`) — click-to-place-vertices polygon drawing, click-near-first-point-or-"Close shape" to finish, inline label+pax form, existing sections listed below with quick label/pax edit and delete. `AssignmentBoard.tsx` implements the brief's core architecture decision: the floor plan renders as a static `<img>`, each saved section becomes an absolutely-positioned DOM `<div>` (`SectionOverlay.tsx`) — **positioned/sized to the polygon's own bounding rectangle** (not the whole image) with `clip-path` remapped into that box's local 0-100% space, so dnd-kit's rect-based hit-testing actually targets each section's own area rather than the full image for every section (an implementation detail the brief didn't spell out but is necessary for the described bbox-not-exact-polygon limitation to mean anything). Both interaction paths write through the same `assignStaff` call: drag-and-drop via `@dnd-kit/react`'s `DragDropProvider`/`useDraggable`/`useDroppable` (current API, not legacy `@dnd-kit/core`, per the brief) with `PointerSensor.configure({ activationConstraints })` giving touch a 500ms delay and mouse an 8px distance threshold (a documented dnd-kit option, confirmed by reading `@dnd-kit/dom`'s actual type declarations rather than assumed); tap-to-pick via `SectionPicker.tsx`, a search-select modal, opened on section click (suppressed for ~200ms after a drag ends via an `onDragStart`/`onDragEnd`-tracked flag, since a completed drag can otherwise still fire a trailing native click on the same element). Pax-capacity warning is a pure frontend `useMemo` comparative signal (flags a section whose own capacity is above the sections' average AND whose assigned/pax ratio is under half the average ratio of sections that do have staff) — soft-only, never blocks, matches the brief's "high-capacity section looks understaffed relative to a low-capacity one" wording rather than a fixed headcount floor.
  - **Nav restructure required and done**: the brief assumed a Home/Scheduling/Floor Plan/People bottom nav already existed in the app — it didn't (`App.tsx` was one flat stacked page, confirmed by grep before starting: zero "Floor Plan" hits anywhere in the repo). Added a minimal `activeTab` state + fixed bottom nav reusing the existing 4 labels, splitting the prior single-page stack across them (Home=Dashboard, Scheduling=ShiftUpload+roster grid, People=StaffDirectory, Floor Plan=new). One regression caught and fixed before it shipped: gating `StaffDirectory` behind the People tab meant its mount-time fetch (which feeds `staffDirectoryByName`, the Scheduling tab's Management-tier cross-reference) would never run unless someone visited People first — fixed by adding an independent `fetchStaffDirectory` call in `App.tsx` on mount, separate from the component's own fetch.
  - **New deps**: `konva`, `react-konva`, `use-image`, `@dnd-kit/react`, `@dnd-kit/dom` (added explicitly since `AssignmentBoard.tsx` imports `PointerActivationConstraints`/`PointerSensor`/`KeyboardSensor` directly from it, not just transitively via `@dnd-kit/react`). Root `tsconfig.json` gained `esModuleInterop: true` — `use-image`'s `export =` CJS shape needs it for the `import useImage from 'use-image'` default-import form; a safe, common Vite-template default, not previously set.
  - **Live-verified against the real Supabase DB** (not just typecheck), all cleaned up afterward: uploaded a real PNG and a real PDF (confirmed the pypdfium2 rasterization path actually runs, not just the image branch) via `POST /upload`; confirmed the saved file is actually reachable through `express.static`; created 3 sections; assigned 4 staff across 2 sections; re-fetched and confirmed the date-scoped join shape; re-posted the same (section,staff,date) triple with a `dutyLabel` and confirmed it upserts (updates the existing row, doesn't 409/500, doesn't reset `status`); published and confirmed exactly the 4 draft assignments flipped to `PUBLISHED` (a 5th, unassigned section correctly untouched); deleted one assignment (204) and confirmed deleting a nonexistent one 404s cleanly; validated 400s on a <3-point polygon, a malformed date, and a nonexistent staffId on assign. All test rows/sections/images and their on-disk files deleted afterward via a throwaway cleanup script (also deleted).
  - Verified clean: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, `npm test` (12/12) — all pass, 0 regressions.
  - **NOT done, explicitly deferred:**
    - Phase 3 notification pipeline (DIY web-push+VAPID vs. OneSignal) — not started at all per the user's explicit gate; `publishAssignments` only flips DB status.
    - Not visually verified in an actual browser — no screenshot/browser tool available in this session (consistent with every prior frontend change in this project's history); verification here was API-level (live curl against real DB) plus typecheck/build/lint, not a rendered-pixel check of the Konva canvas, the clip-path overlays, or the bottom nav layout.
    - Editing an already-saved section's polygon shape (dragging its vertices) is not built — v1 only supports label/pax quick-edit and full delete-and-redraw, which matches the brief's Phase 1 scope ("draw ... label it ... set pax capacity") but is worth flagging as a real gap if venues turn out to want to nudge a drawn shape rather than redo it.
    - `dutyLabel` (optional per the schema/brief) has no dedicated UI — it's threaded through the API and settable via a raw payload (verified live above) but the assignment UI itself doesn't expose an input for it yet.
    - No image-history/versioning UI — re-uploading a new floor plan image creates a new `FloorPlanImage` row and `GET /:locationId` always resolves the most recent one, but old sections tied to a superseded image become unreachable through the normal UI (still in the DB, just orphaned from the current view) rather than being explicitly migrated or archived.
- [x] SAVED the real Bar des Prés venue floor-plan diagram as a permanent reference/test asset for the Floor Plan feature (not a delete-after-verification throwaway like the roster PDFs) — `server/test-fixtures/bar-des-pres-floor-plan-sections.pdf` (named distinctly from the pre-existing `bar-des-pres-roster.pdf`, which is an unrelated rota/shift-parsing fixture for a different feature). Recovered from the user's local `Downloads/Resume/BDP - Table numbers & Sections.pdf` (the attachment reached this session as extracted text + rendered page images, not a direct file path, so it had to be located on disk and copied in byte-identical — verified via matching `%PDF-1.3` header and file size). Genuinely 2 pages, text-layer present (confirmed via `pdf-parse` — 702 chars extracted, table numbers and section pax lines both present), 2 plan versions with slightly different pax numbers per section (a real discrepancy the venue itself has, not a scan artifact), consistent with the kickoff brief's own note to "use whichever the manager sets at draw time, not a hardcoded number."
  - **Reference ground truth** (ratified by the user, reconciling the two plan-version numbers) — the standing answer key for verifying the section-drawing tool, the assignment board, and the pax-capacity warning end-to-end, including the still-outstanding browser/screenshot verification pass (no screenshot tool was available in this session):
    | Section | Pax | Tables |
    |---|---|---|
    | 1 | 18 | 10, 11, 12 |
    | 2 | 16 | 20, 21, 22, 23, 24 |
    | 3 | 15 | 30, 31, 32 |
    | 4 | 14 | 40–53 (bar-height run; plan versions disagree 14 vs 15 — use 14) |
    | 5 | 16 | 60–64 (plan versions disagree 15 vs 16 — use 16) |
    | 6 | 16 | 65–69 |
    | 7 | 16 | lounge cluster 70–73 |
    | 8 | 14 | 74–77 |
    | Bar seats | 10 | (not a numbered floor section — the bar itself) |
  - Not yet built: a seed script that actually draws these 8 polygons against the uploaded PDF and writes them via the real API (mirroring `seed-gattopardo.ts`'s role for the roster-parsing feature) — deliberately not created this round since it wasn't asked for and would mean guessing polygon vertex coordinates from the diagram image rather than a manager actually drawing them; worth building once someone is ready to do the real end-to-end pass (upload this PDF through `SectionEditor`, draw against this answer key, verify pax warnings against known headcounts).
- [x] RAN the real browser end-to-end pass against the Bar des Prés fixture (closing the "not visually verified" gap flagged above) — driven via `playwright-core` + system Chrome (headless), the same pattern this project used previously for the "Failed to fetch" investigation; installed to the session scratchpad only, not added as a project dependency. Uploaded the real PDF through the actual `SectionEditor` upload input (confirmed the PDF→PNG rasterization path, not the image branch), drew all 8 sections by clicking real canvas points against the answer key's pax numbers (simple axis-aligned rectangles approximating each table cluster's rough position — a testing shortcut, not a claim that the tool only supports rectangles; the polygon capture code itself is unconstrained), then assigned staff and published, all through the real UI (tap-to-pick, not a raw API call).
  - **A genuine bug was caught and fixed in the process**, not just a script issue: `AssignmentBoard.tsx`'s `todayIso()` used `new Date().toISOString()` (UTC), which is a calendar day behind local time for the first ~4 hours of each day in Dubai (UTC+4) — assignments made in that window were being written under *yesterday's* date. Root-caused via a direct DB query (assignments existed under `2026-08-24` when the local/system date was `2026-08-25`), not guessed. Fixed to build the date from local `Date` components instead. Re-ran the full pass after the fix and confirmed assignments landed under the correct local date and the publish response echoed it back correctly (`"12 assignments published for 2026-08-25"`).
  - **Two of my own test-script bugs were also caught and fixed along the way, kept separate from the app-code finding above:** (1) checking for the upload prompt immediately after `.fp-tab` appeared raced the tab's own loading-spinner state (`.fp-tab` renders during the spinner too) — fixed by waiting for either the real upload-card or canvas state first. (2) added a `page.reload()` step to verify server-confirmed (not optimistic client) state, but this app's bottom-nav tabs are plain React state with no URL routing, so a reload resets to the Home tab — fixed by re-clicking "Floor Plan" after reload. A run that failed on this second bug (session ended before it could be fixed) was independently checked via direct DB query at the time and confirmed all 12 assignments + all 8 sections had in fact written correctly regardless — the failure was purely in the test script's own follow-up verification step, not a sign of a data-loss bug.
  - **Pax-capacity warning visually and numerically confirmed working exactly as designed**, read from post-reload (server-confirmed) DOM state: Section 1 (18 pax, the largest section, deliberately left with 0 staff) → `⚠ 0/18`, `warned=true`. Sections 2/5/6/7 (16 pax each, all above the 8-section average of 15.625) were staffed adequately (3, 2, 3, 2 people respectively) → all `warned=false`, proving adequate high-capacity sections don't false-positive. Section 8 (14 pax, below the average) was **also** left with 0 staff, deliberately, to prove the warning is comparative rather than "any empty section warns" — correctly `warned=false` despite being fully empty, because it isn't a high-capacity section relative to the others. Sections 3/4 (15/14 pax, below average, lightly staffed) also correctly never trigger regardless of their own ratio, since they're excluded from eligibility by capacity alone. Screenshots (`assignment-board-pre-publish.png`, `assignment-board-post-publish.png`, sent to the user, not committed to the repo) visually confirm: the red-bordered `⚠ 0/18` section 1 overlay, gold-bordered adequately-staffed overlays with real assignee chips and remove buttons, the correct local date in the date picker (`25/08/2026`), and the green publish-success banner reading the correct count and date.
  - **One honest visual side-effect worth flagging**: because my quick test polygons were simple rectangles approximating cluster positions rather than hand-traced irregular shapes, a couple of adjacent test sections (4/5/6, whose boxes were placed touching/slightly overlapping) show some visual crowding/label overlap in the screenshots. This is an artifact of my rough test coordinates, not a defect in the drawing tool — a real manager tracing the actual non-overlapping table-cluster outlines by hand wouldn't produce this. Not fixed/reshot since it doesn't affect correctness of what was being verified (the warning logic, the write paths, the publish flow).
  - All test data cleaned up afterward exactly like every other live pass in this project: 8 sections, 12 assignments, and the uploaded PNG (both the DB row and the on-disk file) deleted via a throwaway script; both dev servers stopped and ports confirmed closed; the two headless Chrome process trees spawned by the two full runs (one that crashed mid-script on the reload bug, one that completed cleanly) identified precisely by their `--headless` command-line flag (to avoid touching the user's own real Chrome windows, which were running concurrently) and terminated. The PDF fixture and the two screenshots are the only artifacts kept.
- [x] AUDITED (on request, ahead of the Lovable UI restyle) whether the schema already supports a section name distinct from "Section N", a free-text note field, and pax capacities above the 14–18 range already tested. Checked the actual schema/route/component code, not memory: `label` was already unconstrained free text (only a non-empty check; "Section 1" was just my own test data, not a format rule) and `paxCapacity` was already an unbounded `Int` (only validated `>= 0`) — both already fully support what was asked, zero changes needed. A `notes` field genuinely did not exist anywhere (schema, routes, or frontend) — confirmed as a real gap, distinct from `SectionAssignment.dutyLabel` (a per-staff-member/per-shift duty tag, not a per-section venue note).
  - **ADDED** `FloorSection.notes String?` (nullable, additive — migration `add_floor_section_notes`, zero impact on existing rows) and threaded it through exactly the surface area named as in-scope: `server/src/routes/floorPlan.ts` (create + update handlers, plus the `/assignments` read-shape so a note is available wherever a section already is, without a second round-trip), `src/api/floorPlan.ts` (`FloorSectionDto`, `AssignmentSectionDto`, create/update input types), and `src/components/FloorPlan/SectionEditor.tsx` (a notes textarea in the new-section modal, alongside label/pax; a matching quick-edit input in the saved-sections list, same blur-to-save pattern as label/pax). One small addition outside the named 3 code files: a `.fp-notes-input` CSS rule in `src/styles/global.css`, needed for the new textarea/input to actually render sized correctly — same file every other Floor Plan class already lives in, not a new part of the app.
  - Did NOT touch `AssignmentBoard.tsx`/`SectionOverlay.tsx` (surfacing the note visually in the daily view, e.g. a tooltip) — the API now returns it there too so that's a small follow-up whenever wanted, not required for the field to exist and round-trip.
  - Live-verified against the real Supabase DB (not just typecheck): created a section with `label: "Terrace West"`, `paxCapacity: 34`, `notes: "Shade after 17:00"` — all three round-tripped correctly through both `GET /:locationId` and `GET /:locationId/assignments`; `PATCH` verified for both updating the note and explicitly clearing it to `null`. Confirms the pax-range and custom-label questions were correctly answered as "already supported" (34 worked with no code change) and the notes gap is now closed. Test section, image, and uploaded file deleted afterward; server stopped, port confirmed closed.
  - Verified clean: `npm run typecheck`, `npm run server:typecheck`, `npm run build`, `npm run lint`, `npm test` (12/12).
- [x] WIRED the restyled Lovable Floor Plan UI (`voyagerventuresdxb-max/shiftsync-dubai-luxury`, branch `main`, cloned to session scratchpad only — never added to this repo, same "component/token source, not a framework adoption" treatment as the earlier Dashboard port) into the real, already-verified backend. Read the actual components (`FloorPlanCanvas.tsx`, `FloorPlanView.tsx`, `FloorPlanEditor.tsx`, `lib/floorplan-state.ts`) before writing anything — the redesign turned out to be a small-pin-marker treatment (MapPin icon + compact "Sec N" badge + primary-assignee first name, tap to select) over a bottom detail card, built against Lovable's own mock state (`useFloorPlan()`/`localStorage`), not our API.
  - **Explicitly did NOT adopt**, per "don't rebuild our data layer to match anything Lovable assumed differently" — each one is a real mismatch, reconciled by adapting the JSX to our shape rather than the reverse:
    - **Point-position pins** (`Section.x`/`y`, free-drag repositioning) — contradicts the kickoff brief's core architecture decision (freeform polygon capture, DOM-overlay-clipped-to-polygon as the real dnd-kit drop target), which is already built and E2E-verified. Reconciled by keeping the polygon/bbox/clip-path droppable exactly as-is and computing the polygon's own **centroid** as the pin's anchor point — same verified data and drop geometry, only the visual treatment (small pin instead of a large filled/bordered region) changed.
    - **AM/PM shift-block dimension**, **`tables: string[]`** (explicit table-number lists per section), and **`notifyAll()`/`BellRing` "Notify"** — none exist in our schema/API. Dropped entirely rather than invented: our `SectionAssignment` stays `shiftDate`-only (no shift-block column), no table-list field was added, and no notify UI was ported at all (Phase 3 is explicitly gated — this isn't even a disabled placeholder, it's just absent, consistent with the original build's dropped `PredictiveBanner`-style placeholders).
    - **Preset `duties` dropdown** — our `dutyLabel` is free text with no fixed vocabulary; inventing one wasn't asked for here, so it's still unexposed in the UI (same gap noted in the original build entry).
    - **Single-assignee-per-section** assumption (Lovable's `Assignment.key` is `date|block|sectionId`, i.e. one slot) — ours already supports many staff per section per day (verified twice via live E2E). Reconciled the pin's "staff first name" display as `firstName(first assignee) + " +N"` for N others, not a single fixed slot.
  - **Ported as genuine restyle** (adapted to consume our real `FloorSectionDto`/`AssignmentSectionDto`/`AssignmentDto` props, not Lovable's types): `src/components/FloorPlan/SectionOverlay.tsx` rewritten — same droppable/bbox/clip-path/centroid math as before, now rendering a small pin (MapPin icon, `sectionPinName()`-derived "Sec N" badge, primary-assignee label, compact pax-ratio tag) instead of a large always-expanded filled box with all chips visible; tapping now opens a new **`SectionDetail.tsx`** (the "tap-to-expand for full details" panel: label, pax ratio with the warning reasoning as visible text not just color, the `notes` field, the full assignee list with per-person unassign, and an "Assign staff" entry point into the unchanged `SectionPicker.tsx`) instead of going straight to the picker. `StaffChip.tsx` and `SectionPicker.tsx`'s rows restyled with the same avatar-initials treatment Lovable uses everywhere staff appear. New `src/components/FloorPlan/staffFormat.ts` holds tiny pure display helpers (`initials`, `firstName`, `sectionPinName`) — reimplemented locally from reading Lovable's originals (same logic), not imported from their module, since importing would mean depending on their mock data types.
  - **Zero changes** to `SectionEditor.tsx` (the Konva polygon-drawing tool, admin setup) or the schema/routes/API client — Lovable's redesign never touched section *drawing*, only the daily-assignment *display*, so the already-verified capture/data layer had nothing to reconcile.
  - **Styling mechanism**: ported Lovable's Tailwind utility classNames close to verbatim (`bg-accent`, `text-warning`, `border-border-strong`, `panel`, `eyebrow`, etc.) — turned out to be a non-issue because `src/styles/tailwind.css` already defines every one of these tokens, pinned to the exact same `global.css` palette, from the earlier Dashboard port ("one source of truth, not a second Lovable palette" — that constraint paid off directly here). Removed the now-dead hand-written CSS these Tailwind classes replaced (`.fp-section`, `.fp-section-over/-warn/-head/-pax/-staff`, `.fp-assignee-*`, `.fp-chip*`) from `global.css`; kept `.fp-picker*` (still the shared modal/detail shell for both `SectionPicker` and the new `SectionDetail`) and `.fp-section-list`/`-row`/`.fp-pax-input`/`.fp-notes-input` (still used by the untouched `SectionEditor.tsx`).
  - **Live E2E-verified again, end to end, through the real restyled UI** (not just typecheck) — same fixture, same answer key, same understaffed/adequately-staffed pax-warning design as the original pass, plus the section-1 note ("Shade after 17:00") added this time to also exercise `notes` through the new detail panel: uploaded the real PDF, drew all 8 sections via the unchanged Konva tool, assigned 12 staff via the new two-level tap-pin→detail→"Assign staff"→picker flow (not the old single-tap-to-picker), reloaded to read server-confirmed state, confirmed via direct pin-content read that Section 1 (0/18, high-capacity) is the only one flagged and Section 8 (0/14, low-capacity, also empty) correctly is not, opened Section 1's detail panel and confirmed the note text and warning explanation render together, published (12 assignments, correct local date), and screenshotted all three states. One test-script bug hit and fixed along the way (not an app bug): the initial `page.goto`/`page.reload` calls used `waitUntil: 'networkidle'`, which hung because Vite dev's HMR client keeps a persistent WebSocket open — switched to `waitUntil: 'load'` plus the script's own explicit `waitForSelector` calls, which is what the whole approach already relied on for correctness anyway.
  - Test data (8 sections, 12 assignments, uploaded PNG — DB rows and on-disk file) deleted afterward; both dev servers stopped and ports confirmed closed; an orphaned headless Chrome process tree from one failed script attempt (same `--headless`-flag identification method as before, to avoid touching the user's real Chrome windows) found and terminated; the scratchpad clone of the Lovable repo deleted now that porting is done. Three screenshots sent to the user, not committed to the repo.
  - Verified clean: `npm run typecheck`, `npm run build`, `npm run lint`, `npm test` (12/12).
- [x] INVESTIGATED (report only, no code changed, per explicit ask) whether parsed shift time/date data survives the confirm-and-commit flow into a real Scheduling/Weekly Rota Builder view. Two premise mismatches flagged before answering, same discipline as the "Failed to fetch" investigation: (1) the live Confirm screen (`ShiftUpload.tsx`) does **not** show only Name+Role — its table already renders Date/Start/End/Break/Status per shift, full data; (2) **no "Weekly Rota Builder" screen exists anywhere in this codebase or its docs** — grepped `src/`, this file, and the PRD docs, zero matches; the 6 Lovable components actually merged (`MEMORY.md` #108) don't include one, and the would-be upload/confirm mock (`TemplateIngestion`) was dropped as redundant with `ShiftUpload.tsx`. **Finding: nothing is being dropped.** `persistShifts.ts` writes the full `Shift` row (date/startTime/endTime/breakMinutes/managerNotes) server-side; `App.tsx`'s `handleCommitted` builds full `Shift` objects client-side and merges them into `mergedRoster.shifts`, already rendered by the existing Mon–Sun "Roster" grid section in `App.tsx` — that grid is already, today, pre-filled straight from confirm. **The real gap found instead:** no `GET` route exists anywhere in the backend (logged as a Next Sprint Goal above) — the grid is session-only, not reloadable from the DB. Proposed (not built): if the Confirm UI is later simplified to Name+Role-only, that should be a purely presentational change to `PreviewReview`'s table, since `PreviewRow`/`onCommitted` already carry full data regardless of what's displayed; a real reloadable rota view needs the missing `GET /api/schedules` endpoint, not a data-flow change. Could not assess the actual Lovable Weekly Rota Builder's expected prop shape since it isn't located anywhere accessible — flagged rather than guessed at.- [x] FINAL FIX WAVE for the `feature/global-chrome-grouping` plan (8 findings from the whole-branch review), closing out the global-chrome/role-grouping port. **(1) Branch now actually builds from a clean checkout** — 23 pre-existing untracked source files (`src/api/`, `src/components/` incl. `FloorPlan/` and the `shiftsync/` panels Dashboard composes, `src/lib/utils.ts`, `engine/rosterView.ts`, `engine/commitBinding.test.ts`) plus the engine types they need (`SwapRequest`, `Employee.needsRoleReview`, `Shift.requiredRole`) were committed as honestly-labeled `wip:` restoration commits, completing what `3043e22` started; verified in a real `git worktree` checkout, not the dirty tree. **(2) Tailwind wiring committed** (`main.tsx` import + `@tailwindcss/vite` plugin) — without it the whole chrome layer rendered unstyled. **(3) AppShell is now a pathless layout route** rendering `<Outlet/>`, mounted exactly once for all five pages instead of once per route: previously every route rendered its own `<AppShell>`, so React remounted it (and RadialDock) on every navigation, killing the dial's spring animation and resetting the voice toggle and notification state. Per-page header text moved to each route's `handle` (typed `RouteHandle`, read via `useMatches()`), defined in `router.tsx` rather than exported from route files — exporting them tripped `react-refresh/only-export-components` and would have broken Fast Refresh on all five pages. **(4) RadialDock index math fixed**: `STATIONS` was hardcoded `5` against a 4-tab array so the mod-5 wrap and mod-4 snap disagreed and the dial could park on a phantom station; now derived from `tabs.length`. **(5) `activeIndex` no longer lies** — was `Math.max(0, findIndex())`, so `/profile` rendered Home as focused with `aria-current="page"`; now `-1` when unmatched, with the dial's numeric spring target kept separate so it just rests. **(6) Six runtime-imported packages moved out of `devDependencies`** (`lucide-react`, `clsx`, `tailwind-merge`, `tailwindcss`, `@tailwindcss/vite`, `tw-animate-css`) — all are imported by shipped code or required to compile production CSS. **(7) `AppStateContext` value + its three handlers memoized** (`useMemo`/`useCallback`) so consumers stop re-rendering on unrelated provider state changes. **(8) AppShell header reads the real venue name** from `useAppState().config.name` for the eyebrow default and derives the avatar letter from it, replacing hardcoded `'Demo Venue'`/`'S'`. Verified clean: `npm run typecheck`, `npm run lint` (0 errors), `npm run build`, `npm test` (20/20) — in both the working tree and a fresh `git worktree` checkout, where the clean-checkout build produced byte-identical asset hashes. **Known pre-existing blocker, NOT introduced here and left alone:** `npm ci` cannot succeed in any checkout because `ruflo`→`@claude-flow/cli`→`@agntcy/slim-bindings` lists three optional platform packages (`linux-arm64-musl`, `linux-x64-musl`, `win32-x64-gnu` @ `2.0.0-alpha.5`) that return 404 from the npm registry; `npm install` works and leaves the lockfile byte-identical (so the lockfile is reproducible), and this is a swarm-tooling devDependency wholly unrelated to the app.
- [x] BUILT the Scheduling phase (12-task plan, `.superpowers/sdd/2026-08-26-scheduling/`): Prisma schema (`RotaTemplate`/`RotaPublish` models, `Shift.sidework`), `shifts`/`rota-templates`/`attendance` backend routes, frontend API clients, real live-fetched current week, Personal Rota/Team Matrix real-data updates, RotaBuilder, Shift Editor, and real Hour Tracking — each task built + independently reviewed (diffs at `.superpowers/sdd/2026-08-26-scheduling/review-*.diff`), fix rounds applied where review found Important findings. Sub-entries below cover the pieces not already granular enough in per-commit messages.
- [x] REPLACED the fixed demo week (`WEEK_START = '2026-08-17'` constant in `AppStateContext.tsx`) with a real, live-fetched current week. `src/engine/weekStart.ts` (`currentWeekStart()`) computes the real Monday of the real current week; `AppStateContext` now holds `weekStart`/`setWeekStart` as real state (seeded from `currentWeekStart()`, not a literal), a `weekShifts` array populated by `refetchWeekShifts()` (calls `GET /api/shifts/:locationId?weekStart=`, re-runs on every `weekStart` change), and `mergedRoster` folds `weekShifts` in alongside upload-committed shifts — synthesizing a minimal `Employee` for a real assigned user who has a RotaBuilder-assigned shift but no upload-derived record yet, so the roster grid/matrix render them anyway. Six real mutators added and exposed from `useAppState()`, all funneling through the same `refetchWeekShifts()` after writing so every consumer (RotaBuilder, Shift Editor, Team Matrix, Personal Rota) sees one consistent post-write state: `createRotaShift`/`updateRotaShift`/`deleteRotaShift`/`bulkCreateRotaShifts`/`publishCurrentWeek`/`fetchCurrentWeekPublishStatus`. `bulkCreateRotaShifts` threads `createdById` through so bulk-created shifts get a real actor in `AuditLog`. FIXED alongside: a failed `fetchWeekShifts` for a newly-selected week previously left the *previous* week's shifts rendered (looks like correct data for the wrong week, not an error/empty state) — `refetchWeekShifts` now clears `weekShifts` on failure. Also a sequence-number guard on refetches so a slow response for an older week can't overwrite a newer week's shifts during rapid week navigation. Verified: `npm run typecheck`, `server:typecheck`, `build`, `lint`, `npm test` all clean at each step; live-verified via the real API (see Task 12 entry below) that `weekStart` genuinely drives what `GET /api/shifts` returns.
- [x] BUILT RotaBuilder (`src/components/shiftsync/RotaBuilder.tsx`, mounted on the Scheduling route) — weekly per-day/per-staff grid, drag-and-drop shift moves via `@dnd-kit/react` (`useDraggable`/`useDroppable`, sensor config matching `FloorPlan/AssignmentBoard.tsx`'s existing pattern: 500ms touch delay, 8px mouse threshold), publish/lock state (locked once the viewed week has been published, via `fetchCurrentWeekPublishStatus`), a save-as-template sheet (captures the visible week as weekday-relative `{dayOffset, roleId, userId, start, end, note}` entries — the plan's own disclosed follow-up, wired to `POST /api/rota-templates` built in an earlier task), an "Open shifts" row (unassigned shifts stay visible and remain a valid drop target), and per-shift briefing note/sidework editing inline. FIXED a cold-start bug found during review: the grid's staff rows and new-shift role catalog were built purely from `mergedRoster.employees`, which on a genuinely empty week (no uploads, no existing shifts — the normal state of "next week," exactly when a manager opens the builder) is nobody — the grid rendered only the synthetic "Open shifts" row with no way to add a shift for a real person. Rows now come from the union of `mergedRoster`'s sections and the staff directory (fetched unconditionally on mount), grouped through the shared `roleGrouping` module so directory-only staff land in their real role section. The role catalog for new shifts now reads from the staff directory too (already returns `roleId` per Task 9's directory fix) instead of only the current week's shifts; `openNew` no longer silently falls back to the alphabetically-first role when a person's role can't be resolved — it leaves the field blank so the existing required-field guard makes the manager pick one. Verified: `npm run typecheck`, `build`, `lint`, `npm test` clean; reviewed independently with 1 fix round (the cold-start bug above), clean after.
- [x] ADDED the Shift Editor route (`bc87ddc`) — a dedicated single-shift edit screen sharing Task 6's six mutators verbatim with RotaBuilder (same `createRotaShift`/`updateRotaShift`/`deleteRotaShift` calls, same backend, by construction no drift between the two entry points into the same data). Its role picker sources from `useAppState().staffDirectory` (deduped by roleId/roleName), mirroring RotaBuilder's own directory-first approach — no new roles endpoint needed. Reviewed clean, no fix round needed. Deferred minors (ledgered, not fixed): the edit path leaves a stale `roleId: ''` initializer that's harmless/unused on that path; no toast for the empty-role-catalog edge case (disclosed scope cut for the lighter editor).
- [x] WIRED real clock-in/clock-out attendance data into Hour Tracking (`6d530a0`, fixed further in `922a073`). The Hour Tracker panel now shows real attendance-derived hours from `GET /api/attendance/:locationId/weekly-hours` instead of totting up scheduled-shift hours, plus a clock-in/clock-out control for the currently-viewed employee. FIXED a real user-facing gap found in review: `clockedIn` was local click-state only, seeded `false` with no resync, and `clockIn`/`clockOut` calls had no `.catch` — reloading the page or switching the "Viewing" employee left the button showing "Clock in" for someone genuinely already clocked in (a real click then silently 409s), and an unmatched-upload employee (`upload-emp-<name>` synthetic id) clicking either button got a silent 404. Fix: `weekly-hours` now also returns a real `clockedIn` flag per staff member (has-any-currently-open `AttendanceLog`, computed independent of the viewed week — an open log from a prior week or spanning midnight still counts as clocked in right now), and the panel resyncs its button state from that field on every refetch/employee-switch instead of trusting a locally-flipped boolean; both handlers now surface a visible error via the existing `.error-block` pattern instead of failing silently. Verified: `npm run typecheck`, `build`, `lint`, `npm test` clean; reviewed with 1 fix round, clean after; live-verified via the real API (Task 12 entry below) that `hours` reflects genuine elapsed clock time and `clockedIn` correctly flips on both clock-in and clock-out.
- [x] ADDED briefing note, sidework checklist, and swap-pending status to Personal Rota (`44d9175`). Cards now surface each shift's `briefingNote` and `sidework` checklist, and show a real "swap pending" status (not a placeholder) when an open `SwapRequest` exists against that shift, sourced from `useAppState().swapRequests`. Also resolved a Task 6 rough edge in the same pass: a `weekShifts`-only employee (assigned via RotaBuilder, never seen in an uploaded roster) previously displayed their raw `employeeId` as a name — now resolves a real display name from `staffDirectory`. Team Matrix (`ea9d522`, `a40d7f3`) replaced its AM/PM letter-codes with real formatted shift times (`formatVenueTime`-derived), and cells were widened to grow for wrapped double-shift text instead of clipping it. Both screens read from the same `weekShifts`/`mergedRoster` source as RotaBuilder — synced by construction, not by convention. Verified: `npm run typecheck`, `build`, `lint`, `npm test` clean throughout; reviewed independently, clean.
- [x] TASK 12 — full verification + live end-to-end check + this MEMORY.md update, closing the Scheduling phase. **Verification pass, all clean:** `npm run server:typecheck`, `npm run typecheck`, `npm run lint` (0 errors, the same 1 pre-existing `AppStateContext.tsx` `react-refresh/only-export-components` warning noted since the chrome-layer phase), `npm run build` (829KB main chunk warning, pre-existing, not a regression), `npm test` (23/23), `npm run test:server` (60/60, 2 skipped by design — gated live-Ollama and live-Docling tests, not failures). **Live E2E against the real running server + real Supabase DB, real `seed-location` data, nothing mocked:** created a shift via `POST /api/shifts` for the real current week (`weekStart` computed the same way the app does — Monday of the real system week) and confirmed it in `GET /api/shifts/:locationId?weekStart=`; published the week and confirmed `publish-status` returned `hasUnpublishedChanges: false`; `PATCH`'d the shift and confirmed it flipped to `true`; saved the week as a rota template, applied it to a genuinely empty week (confirmed empty first), confirmed exactly the right shift count (1) landed on the correct day, then deleted the template and confirmed it's gone; clocked a real employee in via `/api/attendance/clock-in`, confirmed `weekly-hours` showed `clockedIn: true` with a live-growing hour total (0 → 0.01 after ~15s), clocked out, confirmed `clockedIn` correctly flipped back to `false` with the elapsed time preserved. Every row created during this pass (2 shifts, 1 template) was deleted via the real API afterward; the attendance log (no DELETE endpoint exists for `AttendanceLog` by design, consistent with the Staff Directory/Shoutouts precedent) was deleted via a throwaway direct-Prisma script, then the script itself deleted. Pre-existing rows from earlier tasks' own review passes (2 already-published shifts on the real current week) were left untouched — confirmed present both before and after, unaffected. Dev server stopped and port 4000 confirmed closed (`kill-port 4000`) afterward. **One honest side effect, not fully reversible:** the real current week already had a `RotaPublish` record from an earlier task's own review pass (2 shifts already `status: published`); this task's publish-status check necessarily called the real publish endpoint too, which `upsert`s that record's `publishedAt`/`notifiedCount` — the timestamp was advanced to this task's real run time as a result. Not a data-integrity issue (the record's shape/meaning is unchanged, just its timestamp), but flagged plainly rather than silently left unmentioned. **NOT verified — explicitly, per the brief, no browser tool exists in this environment:** the drag-and-drop interaction itself, RotaBuilder's visual grid layout, and the Shift Editor's rendering all need a human with a real browser.
- [x] BUILT the Floor Plan AM/PM + notify-tracking + 86 List phase (9-task plan, `.superpowers/sdd/2026-08-27-floor-plan/`), each task built and reviewed independently. Kept the existing freeform-polygon/server-persisted section base untouched throughout — no task in this plan touches `SectionEditor.tsx` at all.
  - **`SectionAssignment` is now period-aware.** `prisma/schema.prisma` added `AssignmentPeriod` (`AM`/`PM`) as a required column on `SectionAssignment` (`@default(AM)`), folded into the row's own uniqueness (`@@unique([sectionId, staffId, shiftDate, period])`) — the same person can now hold one AM assignment and one separate PM assignment for the same section/date without collision, where before a same-day reassignment for the other half of the day would have silently overwritten the first. `server/src/routes/floorPlan.ts`'s list/create/publish endpoints all take a required `period` query/body param and filter/write by it.
  - **Per-person notify tracking replaced the old no-op "Publish day" button.** `SectionAssignment` gained `notifiedAt`/`publishedAt` columns. `PATCH /api/floor-plan/assignments/:id/notify` stamps `notifiedAt` for one assignment (an honest, manager-initiated tracking event — no real push/SMS/WhatsApp send exists anywhere in this codebase, consistent with how Shift/Attendance notify-equivalents already work). `POST /api/floor-plan/:locationId/publish` now flips every `DRAFT` row for that date+period to `PUBLISHED` and stamps both `publishedAt` and `notifiedAt` at once — publish IS the notify event for a freshly-published assignment, with the individual endpoint left for re-notifying one person later (e.g. after a reassignment). `AssignmentBoard.tsx`'s AM/PM toggle (`chip`/`chip-active` tab pair) drives which period's assignments load/write; its publish button now reads "Publish & notify" and shows an honest per-run result banner (assignment count actually published+notified) instead of the old silent/no-op action. `SectionDetail.tsx` shows a per-assignee notified/not-notified badge plus an individual "Notify" button sourced from the same `notifiedAt` field.
  - **REAL audit logging added to `floorPlan.ts`**, a route that previously wrote zero `AuditLog` rows for any of its actions. Assignment create/delete now write `SHIFT_ASSIGNED`; individual notify writes `ASSIGNMENT_NOTIFIED`; publish writes one summary `ASSIGNMENT_NOTIFIED` row (`entityId` = the locationId, since it covers many rows at once, not one) with the published+notified count in its `note`. Brings this route in line with every other mutation route in the codebase, all of which already audit-logged.
  - **FIXED a real duty-label-clobbering bug during Task 6's review**, verified again end-to-end in this task's own live check (see below): the assignment-upsert route couldn't originally tell "a plain drag-drop/tap reassign, no label typed" apart from "an explicit clear of an existing label" — both arrived as `dutyLabel: null/undefined` after a naive truthy-or-null coercion, so re-assigning an already-labeled staff member (the single most common daily action) silently wiped their label every time. Fixed by checking `Object.prototype.hasOwnProperty.call(req.body, 'dutyLabel')` — the row's existing `dutyLabel` is only touched when the request actually sent that key at all; a plain reassign omits it entirely and the upsert's `update` clause becomes `{}`. `SectionDetail.tsx` also gained the inline duty-label editor this fix was needed for (edit-in-place, save-on-blur/Enter).
  - **BUILT the 86 List** as a genuinely new, real, backend-persisted board — a shared out-of-stock list by station, not a per-user/local concept. New `EightySixItem` model (`itemName`, `station`, `status: EIGHTY_SIXED|BACK_ON`, `note`, `eightySixedAt`/`backOnAt` + who-actioned-it ids) and `server/src/routes/eightySix.ts`: `GET /api/eighty-six/:locationId` (active-only by default; `?includeResolved=1` also returns `BACK_ON` items — "what's out right now" is the default view, not a full history dump), `POST /api/eighty-six` (86 an item, writes `ITEM_86D` to `AuditLog`), `PATCH /api/eighty-six/:itemId/back-on` (writes `ITEM_BACK_ON`, 409s if already back on). Frontend: `src/components/FloorPlan/EightySixBoard.tsx` (items grouped by station, 86/back-on actions) mounted as a third tab in `FloorPlanTab.tsx` alongside the existing Assignment Board and Section Editor tabs. "Live" here means real-DB-backed and reloadable, same as every other "live" feature in this codebase (RotaBuilder, Hour Tracking, etc.) — explicitly NOT real-time push/websocket; a second manager's device only sees a change on its own next fetch. Known pre-existing gating condition this plan doesn't touch: the tab is unreachable until a floor plan image + sections already exist for the venue.
  - **Full verification, all clean:** `npm run server:typecheck`, `npm run typecheck`, `npm run lint` (0 errors, the same 1 pre-existing `AppStateContext.tsx` warning), `npm run build`, `npm test` (23/23), `npm run test:server` (60 pass / 2 skipped by design — gated live-Ollama/live-Docling, not failures).
  - **Live E2E against the real running server + real Postgres DB, real `seed-location` staff, nothing mocked** (fixture floor-plan image/section created via direct Prisma — multipart upload itself wasn't in scope for this check): created an AM assignment and a separate PM assignment for the same person/section/date and confirmed both persist independently, with `GET .../assignments?period=` correctly returning only the matching period's row each time. Set a duty label (`dutyLabel: "Expo"`) on the AM assignment, then re-sent a plain reassign with no `dutyLabel` key at all and confirmed the label survived — the Task 6 fix holds end-to-end, not just by code inspection. Individually notified that assignment and confirmed both `notifiedAt` on the row and a real `AuditLog(ASSIGNMENT_NOTIFIED)` row. Created a fresh `DRAFT` assignment on a second date/staff and published that date+period, confirming exactly the one newly-`DRAFT` row got both `publishedAt` and `notifiedAt`, plus a summary `AuditLog(ASSIGNMENT_NOTIFIED)` row with the correct count in its note. 86'd a test item, confirmed it appeared in the active (no-`includeResolved`) list grouped under its station with a real `AuditLog(ITEM_86D)` row, marked it back on, confirmed it left the active view but reappeared with `?includeResolved=1` showing `BACK_ON`, plus a real `AuditLog(ITEM_BACK_ON)` row. All fixture rows (FloorPlanImage, FloorSection, 3 SectionAssignments, 1 EightySixItem, 4 AuditLog rows) deleted afterward and cleanup independently re-queried to confirm zero remaining. **NOT verified — explicitly, no browser tool exists in this environment:** the AM/PM toggle's visual behavior, the drag-and-drop interaction, and the 86 List's on-screen layout all need a human with a real browser.

## Next Sprint Goals (Scheduling phase handoff)
- `weekStart` is now real app-wide state (`useAppState().weekStart`/`setWeekStart`, backed by `src/engine/weekStart.ts`'s `currentWeekStart()`), not a per-screen constant. Floor Plan/People/Identity (the next phase) should read the current week from `useAppState()` if they need one — do not reintroduce a fixed/hardcoded date the way the pre-Scheduling `WEEK_START` constant did; that's exactly the bug this phase fixed.
- RotaBuilder and Shift Editor share the same six `AppStateContext` mutators (`createRotaShift`/`updateRotaShift`/`deleteRotaShift`/`bulkCreateRotaShifts`/`publishCurrentWeek`/`fetchCurrentWeekPublishStatus`) against the same `/api/shifts`/`/api/rota-templates`/`/api/attendance` backend — no separate data path to keep in sync, no drift possible by construction. Any future screen that needs to read/write a shift should extend this same set rather than adding a parallel one.
- Known, documented technical debt from this phase's own reviews (parked deliberately, not silent gaps — see `.superpowers/sdd/2026-08-26-scheduling/progress.md` for the full ruling on each):
  - A published week's `locked` status doesn't clear when a shift is later deleted from it (`publish-status` only checks `Shift.updatedAt`; a delete leaves no row to bump). Needs a deliberate design decision on how deletions should affect publish tracking — not a quick patch.
  - Clock-in has a narrow TOCTOU race: two concurrent clock-in requests for the same user can both pass the "any open log?" check and create two open `AttendanceLog` rows. Low-likelihood (needs a genuine double-click race across devices within milliseconds), self-correcting on the next clock-out, parked rather than fixed — same bug class already fixed once elsewhere in this repo (swap-request approval, `bec8aea`) if a real fix is wanted later.
  - A handful of Minor findings, ledgered not fixed: `DraftShift.roleName` is write-only dead state in RotaBuilder; a same-cell drag-drop fires a no-op `PATCH`; the Shift Editor's edit path leaves a stale `roleId: ''` initializer (harmless, unused); `nameKey` doesn't collapse whitespace/diacritics (pre-existing, shared with `staffDirectoryByName`); `totalHours` in `rosterView.ts` is now fully dead code repo-wide since Hour Tracking switched to real attendance data.
- **Floor Plan phase handoff (for whoever picks up People/Identity/Onboarding next):** `SectionAssignment` is now period-aware — it has a required `period: AM|PM` column, folded into its own uniqueness key (`sectionId, staffId, shiftDate, period`). Anything that reads or writes `SectionAssignment` going forward (a People/Identity screen surfacing "today's assignment," a reporting view, an onboarding flow that pre-assigns a new hire) must account for that dimension — a query that only filters by `sectionId, staffId, shiftDate` will now silently match up to two rows (AM and PM) instead of one.
- The 86 List (`EightySixItem`, `server/src/routes/eightySix.ts`, `src/components/FloorPlan/EightySixBoard.tsx`) is real and backend-persisted, but "live" means real-DB-backed/reloadable-on-fetch, not real-time push — same convention as every other "live" feature in this codebase (RotaBuilder, Hour Tracking, etc.). A second manager's device only sees an 86/back-on change on its own next fetch; there is no websocket/SSE layer anywhere in this repo. Don't assume one exists when building the next feature that touches this list.
- Known, documented technical debt from the Floor Plan AM/PM phase's own reviews (parked deliberately, not silent gaps — see `.superpowers/sdd/2026-08-27-floor-plan/` review files for the full ruling on each):
  - Duty-label edits and real reassignments both go through the same assignment-upsert endpoint and both write the same `SHIFT_ASSIGNED` audit action with no field distinguishing "the person changed" from "only the label text changed" — the audit trail can't currently answer "who was working this section" vs. "who last touched the label" as two separate questions.
  - `AssignmentBoard` remounts/flashes on every mutation's loading state (assign, notify, publish all trigger a full section refetch+re-render rather than an optimistic/targeted update) — functionally correct, visually rough.
  - No Escape-to-cancel on `SectionDetail`'s inline duty-label editor — blur/Enter commit, but there's no way to back out of an in-progress edit without either saving it or clicking away.
  - A synthetic `upload-emp-<name>` actor id (an uploaded roster row that never matched a real `User`) now hits a live FK-500 path when passed as `createdById`/`actorId` on any of the Floor Plan phase's audit-writing calls — previously these calls omitted the actor entirely, so this risk didn't exist. Same profile as an already-accepted risk in `RotaBuilder.tsx`; worth a real fix (404 instead of a raw FK 500, matching the treatment `shifts.ts` already got for `roleId`/`userId`) if it's ever hit in practice.
  - The 86 List's "Back on" and `SectionDetail`'s "Notify" in-flight guards are both single-slot (track one in-flight id, not a set) — clicking two different items' action buttons in quick succession can let the first one's `finally` re-enable a button before its own request is confirmed done, in a narrow interleaving. Inherited from an already-reviewed-and-accepted pattern; fixing both would mean a `Set`-based guard in both places, out of proportion for what's actually a rare double-click edge case.
- **FINAL WHOLE-BRANCH REVIEW fix wave** for the Floor Plan phase closed 4 real gaps the per-task reviews didn't catch (only visible once the whole branch was reviewed together): the 86 List's CSS was entirely missing (added, mirroring existing `.fp-*` patterns); every audit row this phase wrote had a null actor despite full plumbing existing (now threads `useAppState().currentEmployeeId` into all 7 mutation call sites); the individual re-notify button was hidden once an assignment had already been notified once, making the endpoint's own justified use case (re-notify after a later reassignment) unreachable (now always visible, labeled Notify/Re-notify); and neither of this phase's two riskiest pieces (the duty-label preserve/clear contract, AM/PM key isolation) had automated test coverage despite an established real-server/real-DB test pattern already in this repo (`server/src/routes/shifts.test.ts`) — added `server/src/routes/floorPlan.test.ts` covering both. Independently re-reviewed with byte-identical build hashes to the fix report; all 7 findings confirmed addressed, no new blocking issues.
